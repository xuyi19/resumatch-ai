# 2026-09-10

## 毕设可行性评估（ResuMatch AI）
对当前项目做了「能否作为本科毕设」的评估，结论：**选题骨架合格，当前状态不能直接答辩**。

现状盘点：
- 技术栈：FastAPI + SQLAlchemy 2.0 async + MySQL(asyncmy) + LangGraph 多智能体 + sentence-transformers 语义匹配 + Playwright 爬虫 + 原生 HTML 前端。
- 代码量：app 下约 1439 行 Python / 29 个文件 + frontend 2 个 HTML。
- 已实现：简历解析(PDF/DOCX/TXT)、岗位爬取(智联)、四维加权匹配(技能0.4/语义0.3/经验0.2/学历0.1)、LangGraph 四节点诊断流水线(parser→scorer→gap→rewriter)。

主要短板（按严重程度）：
1. **零实验验证** —— 无任何 baseline 对比、准确率/NDCG 评测，答辩必被问死（最致命）。
2. **零文档** —— 无 README、无需求/设计/测试文档。
3. **依赖缺失** —— requirements.txt 未声明 langgraph、langchain、sentence-transformers、playwright、tenacity、pymupdf、python-docx、numpy 等实际在用包。
4. **tests/ 目录为空**，无单测。
5. 无用户登录/权限体系，无数据库初始化脚本，无部署说明。
6. 创新点未提炼包装。

补强优先级建议：可运行性(依赖+README) → 对比实验(baseline: TF-IDF/关键词/纯LLM) → 文档 → 用户体系 → 前端工程化。

## 产出：技术说明文档（毕设素材）
已生成 `docs/ResuMatch-AI-技术说明文档.md` + 同名 `.html`（6 张 mermaid 图 / 15 张表 / 14 段代码）。
覆盖：项目概述(背景·目标·3个创新点) → 需求分析(F01-F12 + 非功能) → 技术选型 → 四层架构 → 数据库设计(ER) → 核心模块(解析/爬虫/匹配算法/多智能体) → 接口设计 → 前端 → 部署 → 已知问题(B01-B07) + 实验验证方案 → 附录。

**写文档过程中新发现的缺陷（已写入 10.1 节）**：
- B-03 `gap_agent` 往 `gaps` 列表追加 `{"summary":...}`，与 `GapItem` 结构不一致
- B-04 `MatchService` 全表 `select(Job)` 后内存排序，岗位量增长即成瓶颈
- B-05 `/resumes/diagnose` 的 `DiagnoseRequest` 未支持 `llm_config`，与其他接口不一致

**重要**：文档编写期间用户改过代码（新增 llm_config 逐层透传链路：API `LLMConfig` → PipelineService → DiagnosisService → DiagnosisState → 各 Agent；`get_llm` 支持 api_key/base_url/model 覆盖并回退 .env）。**写文档/分析前务必重新读一遍当前文件，不要依赖历史 cat 结果。**

## 产出2：问题解决记录.md（用户提供原文，我核对后归档）
用户提供了一份《ResuMatch AI 开发问题解决文档》全文，已保存为 `docs/问题解决记录.md`，并在文首加了**一致性核对表**、文末加了**当前代码结构快照附录**。

核对结论：文档中 14 项关键实现与代码完全对上（to_thread/zhilian_sync、process_with_stats、no-cache 中间件、?rid=、TASKS、HF_ENDPOINT、field_validator 等），仅 `scripts/debug_crawler.py` 未找到（临时脚本，已删）。

## ⚠️ 重要：项目在 9/10 当天持续快速迭代，文档极易过时
- 15:20 时：1439 行 / 29 文件 / 4 个 Agent / 异步爬虫
- 17:12 时：**2223 行 / 33 文件 / 5 个 Agent / 实时爬虫**
- 新增：`api/v1/live.py`（POST /analyze + GET /status/{task_id}）、`services/live_pipeline_service.py`、`crawlers/zhilian_sync.py`（同步版+to_thread）、`agents/nodes/optimizer_agent.py`（第5个Agent，生成优化后完整简历）、`utils/docx_generator.py`（classic/modern/minimal 三模板导出Word）
- **因此上午写的 `docs/ResuMatch-AI-技术说明文档.md` 已过时**：Agent 链路写成 4 节点、无 live 接口、无 docx 导出。需重做第 4/6.4/7 章并新增章节。

**教训（已反复验证）：本项目代码一天内多次变更，任何基于代码的分析/文档都必须先重新读当前文件，禁止依赖历史 cat 结果。**

## 项目价值 + 论文够不够（软工专业视角）评估
**结论**：有价值，但价值在「工程能力证明」而非产品/学术；论文冲及格/良好够，冲优秀不够。

关键区分（重要）：**软件工程毕设 ≠ 计科毕设**。软工评审看需求工程/设计/实现/**测试**/过程管理，不强制要求算法创新。本项目工程集成型选题因此反而适配。

七项评审点现状：需求工程(部分) / 架构设计(达标) / 数据库设计(达标) / 编码实现(达标，后端2315+前端1020行) / **系统测试(缺失，tests空)** / **效果验证(缺失)** / 工程过程(部分，git仅4次提交且同一天)。

三个缺口及补齐工作量：
1. 系统测试 —— 软工**必查项**，缺了直接扣大分。补 30-40 条 pytest（匹配算法单测 + 接口集成测试）约 1 天
2. 实验验证 —— 冲优秀才需要。5 组 baseline + P@10/NDCG@10/MRR，约 2-3 天
3. 需求工程 —— 用例图 + 用例规约，约半天

风险提示：git 只有 4 次提交且全在 2026-09-10 同一天，若老师查版本历史会显得没有迭代过程，需提前想好说辞或补历史。

## 工具经验
- Markdown → HTML 转换：Anaconda python 自带 `markdown` 3.8（托管 python 没装）。转换脚本放在 `C:/Windows/Temp/md2html.py`，支持 mermaid 代码块抽出 + 表格滚动容器 + MathJax/mermaid CDN。中文路径下运行正常。

