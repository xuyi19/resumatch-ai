# ==========================================================
# ResuMatch AI · 后端依赖
# Python 3.11+ · 数据库为内置 SQLite（无需另装数据库服务）
# 安装：pip install -r requirements.txt
# ==========================================================

# ---------- Web 框架 ----------
fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32        # 文件上传（UploadFile）必需

# ---------- 数据库（SQLite 异步，零配置） ----------
sqlalchemy==2.0.52
aiosqlite==0.21.0

# ---------- 配置与数据校验 ----------
pydantic==2.13.5
pydantic-settings==2.15.0

# ---------- LLM / 多智能体 ----------
langgraph==1.2.11               # 诊断工作流编排
langchain-openai==1.6.2         # OpenAI 协议兼容（DeepSeek / 通义 / 智谱等）

# ---------- JD 链接抓取（可选；抓取失败可改用「粘贴 JD 文本」） ----------
playwright==1.62.0              # 安装后还需执行：playwright install chromium

# ---------- 简历文件解析 ----------
pymupdf==1.28.2                 # PDF 文本提取
python-docx==1.2.0              # DOCX 读取与生成

# ---------- 工具 / 日志 ----------
loguru==0.7.3
httpx==0.28.1

# ---------- 测试（可选） ----------
pytest==9.1.1
pytest-asyncio==1.4.0
