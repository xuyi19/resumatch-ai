# 修改日志（Changelog）

> 记录每个迭代的修改内容、遇到的问题与解决方式，便于回溯与汇报。新迭代请在顶部追加。
> 配套文档：[`改造计划.md`](改造计划.md)（迭代清单）、[`ResuMatch-AI-技术说明文档.md`](ResuMatch-AI-技术说明文档.md)（架构与实现细节、第 12 章踩坑精华）、[`论文准备.md`](论文准备.md)（知识点与答辩）。

---

## M43 全站设计语言：Soft Brutalism（2026-09-24）

**需求**：用户明确「不是缺点缀，是整个程序设计风格太素」——从 stylekit.top 选定 **Neo-Brutalist 柔和变体（Soft Brutalism）** 作为全站设计语言：粗墨边框 + 偏移硬阴影 + 按压式交互，保留圆角与品牌渐变的柔。

**修改内容**（[style.css](frontend/src/style.css#L144) 全局层一次生效）
- `.bg-panel`：2px 墨色边框（--c-ink，暗主题自动反转）+ 4px 偏移实心影 + 统一 12px 圆角——贴纸/印刷感
- `button.bg-panel`：hover 抬升（影变大）+ active 按压（位移归零影）——brutalist 标志性交互
- `.bg-accent` 主按钮：品牌渐变 + 2px 墨边 + 墨色硬影 + 按压位移；disabled 降级灰边
- 总览 Hero 大卡同步墨边硬影（[DashboardView.vue](frontend/src/views/DashboardView.vue#L29)）；点阵背景保留（印刷格纹与风格同源）
- 编号顺延：原 M43 多版本对比→M44，发布自动化→M45，引导→M46，面试复盘→M47
- 验证：npm build 过（纯前端，pytest 不涉及）
- **追加（用户反馈「纯白不好看」）**：亮色主题换**奶油暖白色板**——page 奶油底 / panel 暖白卡 / inset 暖灰 / 边线暖调 / 文字暖墨（[style.css](frontend/src/style.css#L12) 亮色组 6 色值）。墨边硬影 + 奶油底 = 高级印刷质感；品牌紫与暗色主题不动，token 层一处生效全站
- **追加（页头统一）**：全站 7 个页面页头统一为「渐变标题（ink→accent）+ 品牌渐变短横线」——总览/发起诊断/简历库/诊断历史/对比视图/模拟面试/设置/诊断报告，与 M43 设计语言成套
- **追加（侧栏图标统一）**：侧栏顶部 R 标与「桌面/Web」形态徽章、底部品牌徽章卡全部改 Soft Brutalism（渐变底 + 墨边 + 偏移硬影），消除果冻渐变残留
- **追加（主色换代）**：品牌色由蓝紫 **#6D5DFC 换焦糖橙 #DB5A28**（暗色提亮 #F07840）——蓝紫与奶油暖底冷热冲突，焦糖橙是 Soft Brutalism + 奶油底的经典搭档；token（--c-accent）+ Arco 十级阶（亮/暗）+ 复选框硬编码残留全部同步，一处换全站生效
- **追加（暗色可读性 + 总览拥挤）**：① 暗色次/弱文本提亮（ink-sub 152→172、ink-faint 105→134，暗底对比度达标）；② LineChart tooltip 由 ECharts 默认白底改主题化（panel 底/ink 字/线框），暗色不再突兀；③ 总览页全区间距 gap-4→gap-5、mb-6→mb-7、趋势图 h-44→h-48——呼吸感
- **追加（主色定版克莱因蓝）**：用户反馈焦糖橙与奶油底仍不搭，三选一（墨绿/克莱因蓝/橙微调）后选定**克莱因蓝 IKB #002FA7**（暗色提亮 #5A82FF）——奶油暖底上的强撞色经典组合，艺术辨识度高；token（--c-accent/accent-hover）+ Arco 十级阶（亮/暗两套）整体换代
- **追加（图标可见度终修）**：① 非激活导航/顶栏图标 text-ink-sub→**text-ink/80**（用主文本 80% 亮度，亮暗两态同时提升）；② 全站 SVG 图标描边 1.8→**2** 加粗（16px 细线图标暗底下视觉重量不足是「看不清」主因）；③ .icon-btn 基色 ink-sub→ink/0.78；④ 暗色次/弱文本第三轮提亮（ink-sub 172→186、ink-faint 134→148）。npm build 过
- **追加（总览页拥挤根修）**：排查发现**全站唯一没有容器留白的页面就是总览**——其他 7 页均为 `max-w-6xl mx-auto px-6 md:px-10 py-8/10` 容器，唯独 DashboardView 根 div 裸奔导致内容贴边贴顶。补齐统一容器 + 区块节奏统一（页头/四大区块 mb-6/7→mb-8），「继续进行」单条时自动占满整行（消除半宽失衡）、条目 px-4 py-3→px-5 py-3.5。npm build 过
- **追加（使用说明页替代「30 秒看示例」）**：删除首页「▶ 30 秒看示例」按钮及其 AnalyzeView `?demo=1` 直达逻辑（示例数据按钮保留）；新建**说明书样式**的 [GuideView.vue](frontend/src/views/GuideView.vue)（路由 `/guide`，与设置/更新日志同级）——快速上手三步大卡 + 六章目录式排版（编号方块墨边硬影：AI 配置/简历管理/岗位与诊断/读懂报告/模拟面试/工作台）+ 原生 details 折叠 FAQ 6 条；侧栏分隔线下新增「使用说明」入口（book 图标），首页按钮替换为「📖 使用说明」。npm build 过

## M47 模板扩充 + 预览美化 + 岗位库紧凑化（2026-09-24）

- **简历模板 +4 套版式级差异**（[docx_generator.py](backend/app/utils/docx_generator.py)，原 10 套同渲染器颜色变体、同质化高）：
  - `timeline 时间轴`（推荐）：每条目两列表格左时间右内容，最贴近主流真实简历版式；`_group_entries` 启发式把扁平行重组为条目（首行含年份拆时间列，>24 字行首非年份的行归入描述）
  - `banner 深色页眉`：顶部整行深蓝色块白字姓名+联系方式，互联网风格
  - `numbered 编号区块`：01/02/03 焦糖橙编号标题 + 求职意向浅橙高亮条
  - `sectionbar 底纹标题`：区块标题通栏浅绿底纹条，沉稳通用
  - 模板总数 10→14，前端模板下拉自动从后端目录加载，无需前端改动；smoke 验证 4 套全部渲染成功
- **简历库 PDF/docx 预览弹层美化**（[ResumeListView.vue](frontend/src/views/ResumeListView.vue#L109)）：统一 Soft Brutalism——墨边 2px + 8px 偏移硬影 + 页头品牌渐变底；PDF 从裸 iframe 改为内框卡片（圆角+内阴影+细边框），下载按钮升级为品牌实心按钮
- **岗位库布局紧凑化**（[JobLibraryView.vue](frontend/src/views/JobLibraryView.vue#L265)）：p-5 大卡 → px-4 py-3 行卡（与简历库同风格，单卡高度约减半）；标题/公司/城市/薪资/来源压为一行徽标流；JD 默认单行截断点击展开（原 3 行截断+展开按钮）；无 JD 显式占位提示
- **批量管理入口强化**：「管理」→「☐ 批量管理」带边框按钮 + tooltip 说明，管理模式激活为品牌实心（此前用户未发现该功能）
- 验证：pytest 86 passed + npm build 过

## M46 简历多版本对比（2026-09-24）

> 原计划编号 M44（岗位库占用 M45 后，发布自动化顺延 M48，本项重编 M46）

- **版本链模型**：新表 `resume_versions`（resume_id / owner_id / content / source[initial|editor|manual] / created_at）；v1 在简历创建时自动落（upload_and_parse 与 save_text 均接 `_add_version`）；每份上限 20 条，超出自动删最旧（`_VERSION_CAP`）
- **端点三件**（[resume.py](backend/app/api/v1/resume.py)）：`POST /resumes/{id}/save-version`（文本有变化才落库并更新 raw_text，返回 {updated, version_count}，长度校验同 upload-text）；`GET /resumes/{id}/versions`（倒序轻量列表，不含全文）；`GET /resumes/{id}/versions/{vid}`（单版全文）；单删/批删简历**级联清理**版本快照防孤儿
- **前端 diff**（[utils/diff.js](frontend/src/utils/diff.js)）：行级 LCS（O(n·m) DP，简历规模无压力）+ `toPairedRows` 双栏配对（del/add 相邻成行对，多出侧补空行）+ `diffStats` 增删行统计，纯函数零依赖
- **对比弹层**（[ResumeVersionsModal.vue](frontend/src/components/ResumeVersionsModal.vue)）：左版本列表（v1..vN 正序编号展示倒序、来源徽标、时间字数），先点选旧版再点新版即渲染**双栏并排 diff**（删红/增绿/空行补位，+N/-N 行统计）；空态三态（无版本/仅一版/未选够）；简历库卡片操作区新增「版本」按钮
- **编辑器保存链路修正**（[EditorView.vue](frontend/src/views/EditorView.vue#L395)）：「⭐ 保存到简历库」先按文件名找同名简历——命中走 save-version 追加版本并提示 vN（**顺带修掉每次保存都新建重复简历的隐患**），未命中才新建；内容相同提示「无需保存」
- 验证：test_resume_versions.py 6 例（v1 自动落/增版本/无变化/owner 隔离/上限 20/级联清理），全量 **pytest 86 passed** + npm build 过

## M44 项目定名「知岗」+ 新 Logo（2026-09-24）

**需求**：项目定名——中文名**知岗**｜英文名 **ResuMatch-AI**；「知岗，意在读懂岗位所求，认清自身所长」，是求职者的岗位需求分析师，以 JD 为标尺抹平信息差。要求全链路落名 + README 收录定位文案 + 重新设计图标。

**修改内容**
- **新 Logo**：靶心 + 命中之矢（「知」字从矢，瞄准岗位所求）——克莱因蓝渐变圆角方块底 + 白色靶环/中点/箭杆/箭羽；同步落地三处：① [favicon.svg](frontend/public/favicon.svg)（新建，此前 index.html 引用的 favicon 实际是 404）；② App.vue 侧栏 Logo / 窄屏顶栏 / 品牌徽章卡三处 R 字母全部替换为内联 SVG；③ [build_desktop.py](backend/build_desktop.py) 的 ICON_SVG 换新（exe 图标链路 Chrome 无头渲染 → ICO，下次打包生效）
- **落名清单**：index.html title / config.py APP_NAME（**.env 里残留 APP_NAME=ResuMatch AI 覆盖默认值，一并修正**）/ useAppMode 默认值 / 侧栏名「知岗 ResuMatch」/ 品牌卡「知岗 ResuMatch-AI」/ HomeView Hero（badge + 中文名行 + 定位文案）/ ChangelogView 副标题 / desktop.py 窗口标题+更新弹窗+日志文案（健康探测串 `ResuMatch AI`→`ResuMatch` 防失配）/ build_desktop.py APP_NAME（exe 名变「知岗 ResuMatch-AI 桌面版.exe」，产物目录与 zip 名不变）
- **README**：标题改「知岗 ResuMatch-AI」，头部三段完整收录用户提供的中英文名说明与定位文案，项目简介内旧名同步
- 验证：pytest 66 过（含 health 探测链路）+ npm build 过
- **追加（暗色备用图标）**：① 新建 [favicon-dark.svg](frontend/public/favicon-dark.svg)——底色提亮为暗色品牌阶（#5A82FF→#2B50D0），白色靶矢图形不变，深色标签栏/任务栏上更醒目；index.html 加 `media="(prefers-color-scheme: dark)"` link 按系统主题自动切换；② build_desktop.py 新增 `ICON_SVG_DARK` + `--dark-icon` 打包参数（`_gen_icon` 参数化接收 svg），分发深色环境时可打暗色 exe 图标；③ 双变体经 Chrome 无头渲染 ICO 链路验证通过（33KB/28KB）；应用内界面 Logo 无需暗色特判（tailwind from-accent 在暗色下自动切换为提亮蓝）

## M45 岗位库：自管理岗位 + 库×简历批量匹配推荐（2026-09-24）

**需求**：侧栏「简历库」下新增「岗位库」，优化岗位获取流程——先做手动添加（连续录入），再支持多岗位×简历批量匹配推荐；自动获取后续迭代。

**后端**（[jobs.py](backend/app/api/v1/jobs.py) + [entities.py](backend/app/models/entities.py)）
- 新表 `saved_jobs`：owner_id/title/company/city/salary/jd_text(2 万字)/source(manual|ai)/url/created_at——create_all 自动建，无需补列登记
- 6 个端点：`GET /jobs/library`（owner 隔离，最新在前）· `POST /jobs/library`（批量，jd 缺失用「公司+岗位」占位文本，title 空白 422）· `PUT /jobs/library/{id}`（exclude_none 部分更新）· `DELETE /jobs/library/{id}` · `POST /jobs/library/batch-delete`（幂等）· `POST /jobs/library-match`（resume_id 或 resume_text 二选一，库内岗位构造 JobItem 池上限 50，**复用 `_rank_for_resume`** 粗筛+LLM 精排，池 id 转回 int）
- Pydantic 可选字段全部 `| None = None` 容忍显式 null（memory 教训落实）

**前端**
- [JobLibraryView.vue](frontend/src/views/JobLibraryView.vue)（/app/jobs）：统一页头规范容器；「＋ 添加岗位」表单卡（岗位名*+公司/城市/薪资一行 + JD 粘贴框，保存后**清空不收起**连续录入，Esc 收起）；匹配操作条（选简历下拉 + 开始匹配 → 卡片右上角分数徽标三档配色 + 推荐理由，**按分重排显示**，degraded 黄条提示）；岗位卡（城市/薪资/来源徽标 + JD 3 行截断展开全文 + 用此岗位诊断/面试（带 JD 跳转，AnalyzeView 新增 `?jd=` 预填）+ 编辑回填 + Modal 确认删除）；空态引导
- 侧栏「简历库」下新增「岗位库」（briefcase 图标）+ 路由 /app/jobs + api/index.js 六个封装
- AnalyzeView 岗位面板结果卡新增「＋ 入库」按钮（source='ai'，入库后置灰「✓ 已入库」防重复）；现 ♡ 收藏不动（后续合并）

**测试**：[test_job_library.py](backend/tests/test_job_library.py) 8 例全过——CRUD 全链路 / title 422 + 显式 null 容忍 / owner 双会话隔离（fixture monkeypatch APP_MODE='web' 让 X-Session-Id 生效）/ 匹配降级排序 / resume_id 路径 + job_ids 过滤 / 简历 404 / 空库引导 / 缺 resume 400。全量 pytest **74 passed**。踩坑：测试断言受同批入库顺序影响，按 title 定位而非 lst[0]；空库用例必须 mock get_llm 防真实 LLM 调用。

**后续**：AI 按简历自动生成岗位入库、localStorage 收藏迁移合并、岗位库导出。

### M45.1 AI 自动入库（2026-09-24 追加）

- 端点 `POST /jobs/library-generate`（[jobs.py](backend/app/api/v1/jobs.py)）：resume_id/resume_text 二选一（抽 `_resolve_resume_text` 与 library-match 共用）→ LLM（temperature=0.7）按简历生成 1-10 个目标岗位（真实典型企业 + 150-400 字完整 JD）→ `_parse_llm_jobs` 围栏剥离/截取容错 → **与库内「公司+岗位」去重**（全重复返回空列表+提示不报错）→ 落库 source='ai'；LLM 失败/无 JSON 输出 502
- 前端 JobLibraryView 操作条新增「✨ AI 生成岗位入库」（选简历后可用，成功后新岗位插入列表顶部）；api.generateLibraryJobs
- 测试 +3 例（围栏 JSON 解析+去重、二次生成全重复提示、LLM 异常/无 JSON 502），全量 pytest **77 passed**
- 追加：模拟面试页 JD 标签行新增「从岗位库选取…」下拉（库非空时显示，一键带入 JD，JD 过短提示补全）；岗位库「添加岗位」表单展开时隐藏空态引导卡并移除底部说明小字

### M45.2 批量导入 + 示例岗位（2026-09-24 追加）

- 端点 `POST /jobs/library/parse-batch`：粘贴多岗位混排文本 → LLM 提取结构化岗位数组（仅解析预览不入库）；LLM 不可用降级 `_rule_parse_batch` 按空行拆段（第一行做岗位名，degraded 提示）
- `POST /jobs/library` 加 `skip_duplicates` 参数：批量导入跳过与库内「公司+岗位」重复条目，返回 skipped 计数；source 白名单加 `sample`
- 前端 JobLibraryView：「⇪ 批量导入」卡（粘贴框 → AI 解析 → 预览勾选列表（全选/取消/摘要展示）→ 导入选中自动跳重）+ 空态卡「🧪 填充 6 个示例岗位」（6 条覆盖后端/前端/算法/数分/测开/PM 的完整 JD 示例，source='sample'）；批量导入或表单展开时同样隐藏空态卡
- 测试 +3 例（LLM 解析预览不入库、降级段落拆分、skip_duplicates 去重），全量 **80 passed**

### M45.3 搜索与筛选（2026-09-24 追加）

- 岗位库新增筛选栏（匹配操作条下方）：关键字搜索（岗位名/公司/城市，不分大小写）+ 来源筛选（手动/AI 生成/面板入库/示例）+ 城市下拉（库内城市去重动态生成）+ 「N/M」计数 + 一键清除筛选
- **筛选子集联动匹配**：有筛选条件时「开始匹配」只对筛选出的岗位打分（library-match 的 job_ids 候选池过滤），文案同步显示「对筛选出的 N/M 个岗位」；匹配前清空旧分数防陈旧
- AI 生成岗位改用独立来源标记 `ai_gen`（与面板入库 `ai` 区分，供筛选；_LIBRARY_SOURCES 白名单同步）；筛选无结果空态（🔍 + 清除筛选按钮）

### M45.4 编辑/删除可见性 + 批量删除入口（2026-09-24 追加）

- 单条编辑/删除 M45 已有但灰色小字不显眼：改为 ✏️/🗑 emoji 按钮并提色（hover 变 accent/红），管理模式下隐藏避免与复选框冲突
- **补齐批量删除前端入口**（M45 仅后端端点，前端漏做）：筛选栏右侧「管理」开关 → 卡片标题显示复选框（选中卡 ring 高亮，全选/全不选作用于筛选子集）→ 底部浮动操作条（已选 N · 红色批量删除按钮 · 取消）→ Modal 确认 → batch-delete 端点；Arco Modal 用 onOk 回调式（confirm 返回 'ok'/'cancel' 字符串，await truthy 判断会导致取消也执行）

## M42 全站风格深化：Soft UI × Fluent（2026-09-24）

**需求**：用户反馈全站界面仍单调，参考 stylekit.top 模板二次深化——在 token/CSS 层全站生效。

**修改内容**
- [style.css](frontend/src/style.css#L144)：背景层升级为「品牌光晕 + 22px 细点阵网格」（Bento 风格标志性格纹，fixed 不随滚动）；`.bg-panel` 卡片加顶部内高光（顶部受光立体感）；`.bg-accent` 主按钮改真渐变（135° 品牌渐变 + hover 覆盖更深渐变保持交互反馈，disabled 还原）
- [HomeView.vue](frontend/src/views/HomeView.vue#L12)：落地页主标题渐变字（ink→accent 对角渐变裁切，暗色主题自动适配）
- 编号说明：原 M42「简历多版本对比」顺延 M43，后续 M44 发布自动化 / M45 引导 / M46 面试复盘
- 验证：npm build 过（纯前端）
- **追加（用户反馈「感觉没变化」）**：M42 首轮改动是底噪级（点阵/柔影），用户停留的「发起诊断」页感知弱——二轮落到组件层：侧栏底部品牌徽章卡（渐变 R 标 + 版本号）；发起诊断页标题渐变字 + 渐变短横线、步骤数字徽章改白字渐变底、上传空态改品牌色粗虚线 + 渐变图标底座。教训：全局 CSS 层改动必须配合用户当前停留页面的组件级可见变化

## M41 总览页 Bento 布局改版 + v0.10.0 发版（2026-09-24）

**需求**：参考 stylekit.top 风格库，用户反馈界面仍单调——选定 **Bento Grid（便当盒布局）** 用于工作台总览。

**修改内容**
- [DashboardView.vue](frontend/src/views/DashboardView.vue) 重构为 Bento 网格：品牌渐变 Hero 大卡（按时段问候 + 最近成绩大数字 + 发起诊断/模拟面试快捷按钮 + 装饰圆）+ 4 张统计小卡（简历/诊断/面试/⭐收藏岗位）+ 趋势双卡 + 记录双卡；全页 rounded-2xl 统一圆角
- 「最近成绩」从统计卡并入 Hero；「收藏岗位」卡接入 M40 localStorage 收藏数
- 发版 v0.10.0：app/__init__.py 与 package.json 版本号同步；[ChangelogView.vue](frontend/src/views/ChangelogView.vue) 站内更新日志新增 v0.10.0 条目（汇总 M32~M41）
- 踩坑：页头与 Hero 快捷按钮重复——页头去掉「+ 发起诊断」按钮，入口收敛到 Hero
- 验证：npm build 过（纯前端）

## M40 岗位收藏夹 + 一键带 JD 开面试（2026-09-24）

- **收藏模块**：[favorites.js](frontend/src/utils/favorites.js)（localStorage `resumatch_job_favorites`，上限 50 条，收藏时快照岗位完整字段含 jd_text）
- **AnalyzeView**：岗位卡右上角 ♡/♥ 收藏切换；岗位面板新增「★ 收藏 (N)」Tab（独立 v-if 链分支，与结果区互斥）；收藏卡支持「用此岗位诊断」「🎤 用此岗位面试」（跳 /app/interview?jd=）
- **InterviewView**：onMounted 读取 query.jd 预填 JD 表单并提示（无简历时仍引导先选简历）
- 踩坑：Vue 模板 `v-else` 之后不能再挂 `v-else-if`（分支链必须 if→else-if→else 有序），否则编译报 "has no adjacent v-if"——导入比对分支由 v-else 改为显式 v-else-if
- 验证：npm build 过（纯前端改动，pytest 不涉及）

## M39 视觉升级：品牌氛围与质感层（2026-09-24）

**需求**：界面风格单调、颜色寡淡，全局美化提升使用舒适度。

**修改内容**（全部在 token/CSS 层，一处生效全站，亮暗主题自动适配）
- [style.css](frontend/src/style.css)：页面底色叠加品牌紫双光晕（`background-attachment: fixed` 不随滚动）；`.bg-panel` 卡片默认柔影（贴纸感→悬浮感）；`.bg-accent` 主按钮品牌色投影 + 顶部内高光（不碰 background-color，hover 机制不受影响，disabled 抵消）
- [App.vue](frontend/src/App.vue)：侧栏 Logo 渐变块 + 彩色投影；活跃导航项由纯色底升级为「品牌渐变底 + 左侧指示条」（.side-item-active）
- [DashboardView.vue](frontend/src/views/DashboardView.vue)：统计卡 hover 抬升（-translate-y + shadow-lg）；「最近成绩」大数字渐变字（bg-clip-text）；统计卡加彩色 emoji 图标（📄🩺🎤🏆）提升辨识
- 踩坑：style.css 追加注释时锚点误配吃掉了 `---------- */` 尾部，导致相邻 .arco-message 规则被浏览器错误恢复吞掉——改注释务必带完整锚文本
- 验证：npm build 过（pytest 不涉及后端改动）

## M38 工作台分数趋势图（2026-09-24）

- **后端**：/history/stats 新增 `diagnosis_trend` / `interview_trend`（最近 10 次有分数记录，时间正序 [{score, date}]）
- **前端**：[LineChart.vue](frontend/src/components/LineChart.vue) 通用折线组件（smooth + 面积渐变 + 空态占位，主题取色同 RadarChart）；工作台统计卡下方新增「诊断分数趋势 / 面试分数趋势」双卡
- **测试**：stats 结构断言补 trend 字段（pytest 66 过，build 过）

## M37 面试会话管理 + README 界面演示更新（2026-09-24）

**需求**：工作台总览上线后发现真实问题——误建/废弃的面试会话（0/6 题未答）堆积在「继续进行」区且无法清理；README 界面演示截图仍是旧版 UI。

**修改内容**
- **后端**：`DELETE /interview/sessions/{task_id}` 删除面试会话（owner 校验、只删 Conversation 不影响诊断记录）；`/history/stats` 的 ongoing_interviews 过滤 0 题未答的空会话（不再进「继续进行」）
- **前端**（InterviewView.vue）：会话列表卡片 hover 显示「删除」按钮（Modal 确认，@click.stop 防误触恢复）
- **README**：简介重写（全流程闭环叙事）+ 界面演示区 8 张截图全部重拍（工作台总览/诊断报告/模拟面试/简历库等新 UI，`scripts/screenshot_readme.py` CDP 驱动 Edge headless 自动截图）
- **测试**（+1）：删除会话幂等、恢复端点 exists=False、空会话不进 stats；pytest 66 过
- 踩坑记录：Edge headless `--screenshot` 在批量循环中 hash fragment 丢失（PS 5.1 传参）；内置浏览器 tab offscreen 时截图不可用——最终方案 CDP（`/json/new` 建 tab + Runtime.evaluate 注入 location.hash + Page.captureScreenshot）稳定可靠

## M36 工作台总览仪表盘（2026-09-24）

**需求**：/app 默认落在诊断发起页，缺乏全局进展视角；新增数据总览页，求职进展一屏掌握。

**修改内容**
- **后端**（history.py）：新增 `GET /history/stats`——一端点聚合简历数 / 诊断总数 / 均分 / 最近诊断 / 进行中诊断（含 task_id 供跳转）/ 面试总数 / 完成数 / 均分 / 最近面试分 / 进行中面试（answered/total）。注意必须注册在 `GET /{task_id}` 之前，否则 "stats" 被当 task_id
- **前端**：新增 [DashboardView.vue](frontend/src/views/DashboardView.vue) 并设为 /app 默认页（原默认改路由重定向）——4 张统计卡（简历/诊断/面试/最近成绩，可点击跳转）、「⏳ 继续进行」区（进行中诊断→结果页、进行中面试→interview?task_id= 恢复）、最近诊断/面试双列列表（点击直达）、空态引导、加载失败显式可重试
- **导航**：侧边栏「总览」入口（dashboard 图标）置于首位；404 兜底路由同步改指 dashboard
- **测试**（test_dashboard_stats.py，+2）：结构断言 + 聚合增量断言。经验：test.db 跨 pytest 运行残留 + upload-text 同名去重会破坏绝对值断言——固定标识数据先清理再取 before 快照断言增量
- 验证：pytest 65 过，npm build 过

## M35 差距条目标记「已解决」（2026-09-24，补产品化计划漏项 F4）

**需求**：改简历时逐条对照差距清单，改完打勾形成个人任务清单；重诊后对比视图联动高亮。

**修改内容**
- **报告页**（ResultView.vue）：差距条目左侧 △ 变可点按钮——标记后变 ✓ 绿底、文本划线、severity 徽标换「[已解决]」；localStorage `gap_done_{task_id}`（条目 key=`dimension|description`），刷新保留
- **重诊对比**（HistoryView.vue）：新增「差距对照」区——Before/After 差距明细双列，Before 已标记项绿色高亮（✓ 已解决 + 划线），顶部统计「已解决 x/y」；After 清零显示彩蛋文案
- **健壮性**：localStorage 读/写全程 try/catch（损坏值、隐私模式不白屏）
- 验证：npm build 通过（纯前端改动，后端无涉及）

## M34 面试评分量化（2026-09-24）

**需求**：总评从纯文本升级为可量化反馈——总分 + 每题得分 + 维度雷达。

**修改内容**
- **schema**（`InterviewSummary`）：加 `overall_score: float|None`（总分 0-10）与 `scores: dict[qid, float]`（每题得分），旧会话缺省容错（前端/导出自动隐藏分数）
- **SUMMARY_PROMPT**：要求输出全部题目评分（要点覆盖度/JD 相关性/表达结构）与总分；注意 prompt 花括号 `{{}}` 转义必须成对出现，单个未转义 `}` 会让 `str.format` 抛 ValueError
- **前端**（InterviewPanel.vue）：总评头部总分大数字（≥8 绿 / ≥6 蓝 / <6 橙）、四类维度雷达（`RadarChart` 新增 `dims`/`max` props 参数化，max=10）、准备区题单卡与聊天流题干消息显示每题得分徽标
- **导出报告**：`generate_interview_docx` 题目标题带「（得分 X/10）」、总评区顶部带总分
- **测试**：fake InterviewSummary 补 overall_score/scores；断言总评响应含分数；pytest 63 通过、build 通过

**延伸：面试会话列表带总分（同日）**
- `GET /interview/sessions` 每项加 `overall_score`（旧会话/未完成为 None）；`/app/interview` 记录列表显示分数徽标（同档位色），旧记录仍显示「✓ 已出总评」

## M33 面试报告导出 Word（2026-09-24）

**需求**：面试模块补最后一环——诊断报告可导出而面试不行。把「问答记录 + 总评」导出为 Word。

**修改内容**
- **docx 生成**（`utils/report_docx.py` 新增 `generate_interview_docx`）：标题（题数+日期）→ 一、面试问答记录（每题：题干加粗 / 我的回答 / 面试官点评，未答显示「（未作答）」）→ 二、整体总评（overall + 亮点/待改进/建议列表）。与诊断报告共用 `_style`/`_para` 样式基建
- **端点**（`interview.py` `POST /interview/export/{task_id}`）：完全照抄诊断导出模式——无会话 404、无总评（未完成）409、blob 下载（文件名 UTF-8 编码）/ `save_path` 服务端直写，两种方式均落 export_history（template=interview）
- **前端**：`api.interviewExport`（blob）/`interviewExportToPath`（桌面直写备用）；总评区头部加「📄 导出面试报告」按钮
- **测试**：`test_export_interview_report`——未完成 409 / 不存在 404 / docx 魔数（PK）与 content-type 校验
- 验证：pytest 63 通过；npm build 通过

**顺手优化：简历库 PDF 预览观感（同日）**
- 预览弹层放大（max-w-3xl/max-h-85vh → max-w-5xl/90vh）+ 遮罩 backdrop-blur + 阴影圆角
- iframe src 加 `#toolbar=0&navpanes=0` 隐藏 Chrome/Edge PDF viewer 自带的灰色工具栏/侧栏，观感干净
- 加载态：iframe `@load` 前显示「正在加载原文件…」，消除白块突兀；头部加类型徽标、关闭按钮 hover 反馈；文本预览限宽居中衬底色

## M32 面试官多轮自由对话（2026-09-24）

**需求**：模拟面试从「严格逐题一答一评」升级为可与面试官自由多轮对话（可被追问、可随时补充、可提前结束本题）。（曾同时实现 TTS 朗读，后按用户要求整体移除）

**修改内容**
- **多轮自由对话（后端 `interview.py`）**：
  - `Conversation` 新增 `chat_log` TEXT 列（`db.py` 补列登记）存完整对话流 JSON `[{role: interviewer|candidate, qid, content}]`
  - 新增 `POST /interview/chat/{task_id}`：候选人围绕当前题随时发言，LLM（`ChatTurn{reply, advance}`）像真实面试官一样自然回应——可肯定/追问/指出补充点，不灌输标准答案；`advance=true`（回答覆盖考察点/明确答完/多轮无新信息）时本题收尾：本题内候选人全部发言合并落 `answers[qid]`（供总评与历史摘要），`current_index` 前进并追加下一题题干入对话流；末题收尾复用 `SUMMARY_PROMPT` 出总评
  - `force_advance` 参数：「本题回答完毕，进入下一题」按钮在 prompt 加【强制收尾】标注，确保收尾不被 LLM 拖回追问
  - `_chat_base_log()`：`chat_log` 优先，旧会话（无 chat_log 的 mock 时代数据）从 questions/answers 合成——GET/start/chat 三条路径统一返回完整对话流，前端单一渲染路径
  - 删除被替代的 `POST /interview/mock/{task_id}`（严格一答一评模式）与 `MockReply`/`RoundFeedback`/`ROUND_PROMPT`；`_history_text` 去掉不再使用的 `upto` 参数
- **前端 `InterviewPanel.vue`**：模拟面试区重构为聊天式消息流（面试官左侧气泡含题号/分类标签、题干高亮、候选人右侧气泡）+ 输入框（Enter 发送）+「本题回答完毕，进入下一题 →」+ 自动滚动到底 + 「面试官正在思考…」指示；进度显示当前题号与分类
- **测试**：`test_interview_free.py` 重写 mock 用例为 chat 全流程（追问留在本题 → 强制收尾推进 → 6 题完出总评 → chat_log 落库一致 → 已结束 400；无会话 404/空内容 422；llm_config 透传）
- 验证：pytest 61 通过；npm build 通过

**踩坑**：fake LLM 用 `"强制收尾" in prompt` 判断收尾标记，而 `CHAT_PROMPT` 固定文案里就有「若下方标注了强制收尾」——首例直接 advance=true 误判。标记统一带【】（`【强制收尾】`）与正文文案区分。教训：**用 prompt 子串做测试判定时，先检查 prompt 模板正文是否已含该子串**。

**Hotfix：简历库批量删除 500（同日）**
- 现象：简历库勾选多份「批量删除」必失败（toast「批量删除失败」），单删正常
- 根因：`resume.py` 的 `delete_resumes_batch` 在 handler 内直接引用 `Resume` 模型，但该模块从未 `from app.models.entities import Resume`——定义时不报错，路由注册正常、其余端点全好，唯独调用该端点时 `NameError` → 500。此端点自 F1 起无任何测试覆盖，坏了一直没发现
- 修复：补 import；`test_api.py` 新增 `test_delete_resumes_batch`（两份上传 → 批删 → 列表确认消失 + 幂等 0 + 空列表 0）。写用例时两份简历用了相同文本，命中「重复上传自动复用」返回同一 id，`deleted==2` 断言失败——改用不同文本

**继续优化（同日）**
- **修复：强制收尾占位文案污染内容**——「本题回答完毕」按钮发送的占位文案此前会落入 `chat_log` 并被合并进 `answers[qid]` 参与总评。现在 `force_advance` 轮不写候选人消息，仅用固定提示语给 LLM；补断言（对话流全程不含占位文案，chat_log 条数 20 → 14）
- **发送乐观上屏**：LLM 回应常需数十秒，候选人消息现在发出后立即显示（失败自动回滚并恢复输入框内容），消除「点了没反应」感
- **本题回答思路**：聊天输入框上方新增当前题 hint 折叠项（💡 展开），答题时不必切回「面试准备」查提示
- **进度条**：题号文本下加进度条（完成题数占比，advance 时动画推进）
- 验证：pytest 62 通过；npm build 通过

---

## M31 模拟面试独立成页 + 面试面板组件化（2026-09-24）

**需求**：把「面试准备 + 模拟面试」从诊断结果页独立出来，用户不跑诊断也能直接基于自己的简历和意向岗位 JD 开一场模拟面试。

**修改内容**
- **后端**（`interview.py`）：
  - 新增 `POST /interview/start-free`：resume_id + jd_text（20-6000 字）直接创建独立面试会话，task_id 用 `iv-` 前缀生成（不与诊断任务 id 冲突），上下文（resume_id/jd_text）存 `Conversation.context`（新 TEXT 列，`db.py` 补列登记）
  - 新增 `GET /interview/sessions`：最近 20 场面试会话列表（独立/诊断来源区分、进度、是否已出总评），支持继续面试与历史回看
  - `_load_context` 双模式：优先按诊断任务取上下文，查不到回退独立会话（从 `context` JSON 读简历与 JD）——出题/答题/总评三端点零改动兼容两种来源
  - `start` 端点加 `regenerate` 参数：「重新出题」真正生效（清空题单重出，不重复消耗会话）
- **前端**：
  - 新组件 `InterviewPanel.vue`：面试准备（题单）+ 模拟面试（逐题对话）两子区合一，诊断结果页与独立页复用；onMount 自动恢复会话
  - 新页面 `InterviewView.vue`（`/app/interview`，侧边栏新增「模拟面试」导航）：简历下拉 + JD 粘贴 + 开始面试；下方「最近的面试」会话列表可继续；`?task_id=` query 直接恢复会话（刷新不丢）
  - `ResultView.vue` 面试相关 Tab 由 4 个合并为 3 个（诊断报告/优化简历/面试），内联模板与脚本整体迁出，页面瘦身约 200 行
- **测试**：新增 `test_interview_free.py` 5 例（start-free 建会话与题单、sessions 列表、参数校验 422/404、无诊断记录走独立上下文全流程答题出总评、start 幂等）+ `llm_config=null` 回归 1 例
- 验证：pytest 59 通过；npm build 通过；浏览器 E2E（选简历 + 粘贴 JD → 出题 → 切题单/答题 → 点评与进度 2/6 → 返回列表出现「独立面试 进行中 1/6」）

**踩坑**：`FreeStartRequest.llm_config` 声明为严格 `dict` 时，前端未配置 Key 显式传 `null` 会被 Pydantic 422 拒掉——表现为「点击开始面试毫无反应」（请求在进 handler 前被拒，无会话行、toast 一闪而过）；curl/fetch 手测时省略了该字段走了默认值，导致多次复现失败。修复为 `dict | None` 并加回归用例。教训：**接口模型的可选字段必须容忍前端显式 null；前端「发了请求但后端无痕」优先怀疑 422**。

**Hotfix：面试 Tab「卡死」（同日）**
- **根因**：`run.py` 看门狗用 `browser_proc.wait()` 单信号判断——专属浏览器进程一退出（用户关掉弹窗改用自己浏览器、或 Chrome 把 URL 转发给已有实例后立即退出）就立刻杀掉前后端。结果页三个 Tab 里诊断/优化是 `v-show`（已渲染内容不受影响），唯独面试 Tab 是 `v-if` 挂载 + onMount 发 GET，服务死亡后该 Tab 表现为「点了没反应」，且错误被静默吞掉——用户感知为「面试 Tab 卡死」
- **修复**：
  - 后端新增 `POST/GET /meta/heartbeat`：前端 App 挂载即上报并每 20s 续报心跳；`run.py` 在浏览器进程退出后轮询心跳，仍有页面在用则继续服务，心跳消失（90s 阈值，覆盖后台标签页 1 次/分钟节流）才关停——「关浏览器自动退出」约束保留，误杀消除
  - `InterviewPanel.vue`：onMount 的会话恢复 GET 加载中/失败状态显式化（失败出横幅 + 重试按钮），不再静默吞错
- 教训：**「某单点功能卡死」而其余页面正常时，先怀疑该功能独有的一次性挂载请求 + 全局进程/服务异常**；看门狗类单信号防护必须区分「信号源退出」与「使用者退出」

**补优化（同日）**
- `POST /interview/mock/{task_id}` 的点评/总评此前硬编码 `llm_config=None`（走服务端默认 Key）——与出题端点不一致，无服务端 Key 时「能出题但答题必失败」。`MockReply` 增加 `llm_config: dict | None`，前端 `interviewMockReply` 与 `sendMockAnswer` 透传 `_llmCfg()`，补透传回归用例
- `call_llm_for_json` 增加 `total_timeout`（默认 150s）总时限：防「模型响应慢/JSON 反复不合法」时重试叠加拖到数分钟（axios 180s 先超时、后端继续空烧）；到点提前抛错，用户可拿到明确报错而非无限等待

---

## M30 数据源精简 + 手动导入公司比对（2026-09-24）

**需求**：① 删除无用的招聘数据源（JSearch/Adzuna 均为海外聚合器，不覆盖国内岗位，配置了也没用）；② 支持用户手动导入目标公司进行匹配比对推荐。

**修改内容**
- **数据源精简**：`job_market.py` 删除 JSearchProvider / AdzunaProvider（连带 `_fmt_salary`、httpx 依赖清理）；`PROVIDER_IDS = ("mock", "ai")`；前端 `JOB_SOURCES` 只留「AI 生成（推荐）/ 示例数据」；`loadJobSource()` 对已删除的 provider 标识自动迁移为 `ai`（旧配置兼容）
- **手动导入比对**：
  - 后端 `POST /jobs/import-match`：resume_text + items（company/title/city/salary/jd，≤20 条）→ 粗筛（Jaccard）+ LLM 精排（0-100 匹配分 + 理由）；JD 缺失时用「公司+岗位名」占位参与粗筛；LLM 失败降级关键词排序不报错
  - 前端分析页岗位面板拆「搜岗位 / 导入比对」双 Tab：录入表单（公司+岗位必填，城市/薪资选填）→ chip 列表（localStorage `resumatch_imported_companies` 持久化，可单个移除）→「与简历比对」出排序结果（与搜索结果共用展示区，可一键回填 JD 开始诊断）
- **重构**：recommend 内联的粗筛+精排块抽成 `_rank_for_resume()`，与 import-match 共用
- **测试**：test_jobs.py 删除 jsearch/adzuna 归因用例，新增 import-match 降级排序 + 参数校验 2 例；工厂测试补「已删除标识回退 mock」断言
- 验证：pytest 54 通过；npm build 通过；浏览器 E2E（示例简历 → 导入阿里云/字节 2 家 → 比对 → 90/60 分正确排序 → 回填 JD）

**说明**：手动导入是「数据源」之外的第三条路线——用户自带候选（目标公司名单），不依赖任何外部接口，天然覆盖国内；与 AI 生成画像、示例数据互补。

---

## M29 诊断卡死修复 + 国内岗位数据源方案（2026-09-23）

**问题 1（诊断卡死）**：一直提示「已有诊断任务进行中」无法发起新诊断。根因：M16 跨重启恢复会把 waiting_clarify 任务重建进内存任务表，而 waiting_clarify 计入「进行中」状态，追问界面又没有放弃入口——不想回答旧追问的用户被永久 409。

**修复**
- 新增 `POST /live/abandon/{task_id}`：内存 + DB 双清，立即释放名额；`abandoned` 标记阻断 run() 的 `_update`/终态落库（防在途复活），pending 瞬间放弃也安全
- 追问界面新增「不想继续了？放弃此任务」（确认弹窗 → 放弃 → 回分析页）
- 409 响应带 `X-Active-Task` 头，AnalyzeView 捕获后弹「前往该任务」引导，不再只有一句死提示
- 发起诊断前自动作废超过 24h 未应答的 waiting_clarify（内存+DB）

**问题 2（一键获取岗位）**：JSearch/Adzuna 均为海外岗位聚合器，不覆盖国内在招岗位——配好 Key 也搜不到国内岗位，属方案级缺陷。

**方案**
- 新增 `AIJobProvider`（provider=ai）：用用户已配置的 LLM 按关键词+城市生成典型岗位「参考画像」（国内公司风格、人民币薪资、完整 JD），明确标注「AI 参考岗位 · 非实时在招数据」；LLM 失败降级内置示例数据
- 前端数据源预设新增「AI 生成（推荐）」置顶（无需 Key）；JSearch/Adzuna 文案标注「不含国内」；`/jobs/search`、`/test-job` 透传 llm_config（Provider.search 同步/异步双兼容）
- 验证：pytest 56 通过（新增 test_jobs_ai.py 4 例）；npm build 通过；实测放弃旧任务释放名额成功、AI 源真实模型返回 10 条国内岗位（阿里/网易/蚂蚁等风格画像）

**教训**
- 「防重复任务」这类防护必须给出口（放弃/超时），否则跨重启恢复类功能会把防护变成死锁
- 竞态：任务创建（内存）与 DB 落库不在同一原子操作内，放弃/状态类操作需以内存为准 + 标记位防覆写

---

## M28 更新日志补全 + 版本号统一（2026-09-23）

**问题**：站内「更新日志」页停留在 v0.5.0（09-11），缺 M11-M27 全部内容；版本号三处不一致（`app/__init__.py` 0.5.0 / `config.py` APP_VERSION 0.1.0 / `package.json` 0.0.0），且 `backend/.env` 显式 `APP_VERSION=0.1.0` 覆盖一切，侧边栏长期显示旧版本。

**修改内容**
- **版本单一事实源**：`config.py` 的 `APP_VERSION` 默认值改为引用 `app/__init__.py` 的 `__version__`（env/config.json 仍可覆盖）；`.env` 删除 `APP_VERSION` 覆盖行；版本统一升至 **0.9.0**（`package.json` 同步）
- **更新日志页**（`ChangelogView.vue`）：补 v0.6.0（双形态+实时诊断）/ v0.7.0（RAG 证据接地+动态追问）/ v0.8.0（全站风格重做）/ v0.9.0（岗位市场+简历库+报告闭环）四档；页头新增「当前 v{{ meta.version }}」徽标（读 /meta）；最新条目带「当前版本」绿标
- 验证：pytest 52 通过；npm build 通过；浏览器实测徽标与时间线（9 版本）渲染正常，侧边栏显示 0.9.0

**教训**：版本号有多处来源时必然漂移——`.env` 里的覆盖项最容易忘记更新；单一事实源 + 其余处只做引用。

---

## M27 简历库 PDF 预览：原文件保留 + 浏览器内原版式渲染（2026-09-23）

**需求**：简历库预览此前只能看提取后的纯文本，PDF 版式信息（排版/分页/加粗）全部丢失；用户要求预览支持 PDF。

**修改内容**
- **后端**
  - `config.py` 新增 `FILES_DIR`（`data/files/`，简历原文件目录）；`entities.py` Resume 加 `file_path` 列（老库由 `db.py` `_SCHEMA_NEW_COLUMNS` 自动补列）
  - `resume_service.upload_and_parse`：上传时 pdf/docx 原件落盘 `FILES_DIR/{resume_id}{ext}`（flush 先取 id 再命名；落盘失败仅告警不阻塞入库）；txt 粘贴简历不存原件
  - `resume.py`：新增 `GET /resumes/{id}/file`（FileResponse，`content_disposition_type="inline"` 供 iframe 直渲，下载名用库内改名后的 filename）；`/list` 返回 `file_type`（pdf/docx/null）；单删/批量删除连带清理落盘文件；`data.py` 一键清空按 owner 的 file_path 精确删原件（web 形态也安全）
- **前端**（`ResumeListView.vue`）：预览弹层 max-w-3xl——pdf 走 `<iframe>` 浏览器原生渲染（保真版式），docx/纯文本仍是文本预览；有原件时弹层右上角「下载原文件」（`<a download>`）；列表卡片名称旁文件类型徽标
- **测试**：新增 `test_resume_file_preview.py` 4 例（PyMuPDF 造文字版 PDF）：上传存原件+file 端点返回、删除连带清文件、文本简历无原件 404、docx 同样可下载；`conftest.py` 补 `FILES_DIR` 隔离到 `tests/.tmp/`
- 验证：pytest 52 通过；npm build 通过；浏览器实测 PDF 弹层渲染 + 下载链接 + 非 PDF 文本预览回退

**踩坑**
- iframe 加载 PDF 报 `net::ERR_ABORTED` 且空白：FileResponse 默认 `Content-Disposition: attachment` 会让浏览器中止内嵌渲染转下载，必须显式 `content_disposition_type="inline"`
- 调试时用 `curl -I`（HEAD）测该端点会得到误导性 404，需用 GET 验证

---

## Hotfix ResultView 白屏：TDZ 变量先用后声明（2026-09-23）

**问题**：点击「开始诊断」跳转结果页后内容区全白（错误还会沿 vue-router 传播，连带其他页面内容区一起白掉）。浏览器 console 报 `ReferenceError: Cannot access 'activeTab' before initialization`。

**根因**：`ResultView.vue` 的 `<script setup>` 中，`watch(activeTab, ...)`（M23 面试 Tab 懒加载）写在了 `const activeTab = ref('diagnosis')` 声明之前——const 存在暂时性死区（TDZ），setup 执行到 watch 那行即抛 ReferenceError，组件挂载失败。

**修复**：将 watch 块移到 `activeTab` 声明之后，并注释说明原因防止回归。浏览器实测：结果页（waiting_clarify 追问界面）、设置页、首页均正常渲染，console 无业务报错。

**教训**：`<script setup>` 里 `watch/computed` 引用的 ref 必须声明在前；白屏且 console 有 "Cannot access ... before initialization" 时先查 TDZ。

---

## M24 F1 简历库：独立管理页（2026-09-23）

**修改内容**
- **后端**（`api/v1/resume.py`）：新增 `GET /resumes/list`（列表 + 每份简历最近一次成功诊断的综合分/时间，注册于 `/{resume_id}` 之前避免 int 转换器拦截）、`POST /resumes/rename/{id}`（filename 校验）、`DELETE /resumes/{id}`；`GET /resumes/{id}` 补 `raw_text` 字段（仅该端点返回，供预览）
- **前端**：新增 `ResumeListView.vue`——卡片网格（名称/更新时间/字数/最近诊断分三色），操作：发起诊断（带 `?resume_id&resume_name` 跳分析页）/ 预览（弹层显示原文）/ 重命名（prompt）/ 删除（二次确认）；路由 `/app/resumes`；`App.vue` 侧边栏「简历库」从 disabled 占位转正式入口
- **AnalyzeView 联动**：支持 `?resume_id=` 预选——提示条显示已选简历、提交时直接复用该简历跳过重复上传（校验只查 JD）；「创建简历 → 保存 → 从简历库发起诊断」链路闭环
- `api/index.js` 新增 listResumes / getResume / renameResume / deleteResume
- 验证：pytest 48 通过；npm build 通过

---

## M23 面试功能落地：面试准备 + 模拟面试（2026-09-23）

**修改内容**
- **面试准备**（新增 `api/v1/interview.py` + `ResultView.vue` Tab3 重写）：基于简历+JD+诊断差距出 6 题（四类覆盖，附考察点 focus 与回答思路 hint），题单卡片 + hint 折叠展示 + 重新出题；题单存 `Conversation(type="interview")` 复用 M17 表（设计时预留 type=interview）
- **模拟面试**（Tab4 重写）：`POST /interview/mock/{task_id}` 逐题对话——候选人回答 → LLM 点评（feedback 先肯定后建议 + transition 面试官衔接语）→ 全部答完出总评（overall + 亮点/待改进/建议三栏）；`GET /interview/{task_id}` 恢复会话（切 Tab 懒加载）；前端逐题轮次卡 + 进度条 + 总评卡
- `api/index.js` 新增 interviewStart / interviewGet / interviewMockReply；llm_config 从 localStorage 透传
- 验证：pytest 48 通过；npm build 通过

**备注**
- 至此报告页四个 Tab（评分报告/对答优化/面试准备/模拟面试）全部为真实功能，无占位空壳

---

## M22 收尾：数据信任 + 更新检查 + 体验拾遗（2026-09-23）

**修改内容**
- **C1 数据与隐私**（新增 `api/v1/data.py` + `SettingsView.vue`）：`DELETE /api/v1/data` 清空本用户诊断记录/简历库/导出历史/会话（按 owner 隔离）+ 证件照目录（仅 local 形态）+ SqliteSaver 快照（按 thread_id）；设置页新增区块说明本机存储与 LLM 服务商数据流向，清空按钮二次确认
- **C2 更新检查**（`desktop.py` + 根 VERSION）：服务就绪后后台线程拉取 Gitee/GitHub 仓库 VERSION（5s 超时静默），有新版 webview 原生弹窗提示（只提示不自动下载）
- **C3 体验拾遗**（`ResultView.vue`）：追问提交校验改为逐条指出空着的问题编号
- 测试：`tests/test_clear_data.py`（清空 + 幂等 2 项断言）；修复 data.py `execute()` 参数顺序语法错误（pytest 收集期捕获）；全量 48 通过；npm build 通过

**备注**
- 产品化完善计划（P0 闭环/风格重做/P1 容错/P2 收尾/F 扩展）至此全部实施完毕；VERSION 文件随发布更新

---

## M21 容错 + 启动体验：失败重试 + 扫描件引导 + 一键启动降噪（2026-09-23）

**修改内容**
- **B1 失败保留部分结果**（`services/diagnosis_service.py` + `live_pipeline_service.py` + `ResultView.vue`）：交互模式 diagnose 异常时从 SqliteSaver 快照恢复已完成节点结果抛 `DiagnosisPartialError(partial, cause)`；`_flow` 捕获后组装部分 result_data（`partial: true` 标记）与 error 并存落库并推 SSE；失败任务不再释放 checkpointer 快照；前端失败态显示「已产出的部分结果（未丢弃）」清单（综合分/差距数/建议数）
- **B1 同任务断点重试**：新增 `POST /live/retry/{task_id}`——仅 failed + 快照存在可重试，`DiagnosisService.resume_after_error`（graph `ainvoke(None)` 断点续跑）从失败节点继续，已完成节点不重跑；续跑成功走 `_finalize` 正常落库，再次失败同样保留 partial；前端失败态「🔁 从失败处继续」按钮留在报告页重置轮询（`api.retryLive`）
- **B2 扫描版 PDF 明确引导**（`utils/file_parser.py`）：`parse_pdf` 返回 (text, image_only_pages, total_pages)；整本文本 <50 字且几乎全图片页时抛特定文案「检测到扫描版/图片型 PDF，无法提取文字，请换文字版 PDF，或先完成 OCR」替代笼统「解析后为空」
- **run.py 一键启动优化**：移除内网穿透（cpolar）问答与 MySQL 提示残留；conda 路径修正为 `E:\conda\envs\resumatch-ai\python.exe`；后端加 `--no-access-log`（只留启动横幅与错误）、前端 vite `--logLevel warn`——服务窗口日志大幅降噪；主窗口 ANSI 配色紧凑横幅输出，退出改为统一终止两个子进程
- 测试：`tests/test_file_parser.py` 2 用例；修复重构引入的 `_run_interactive` 未 await 问题（test_live_recovery 捕获）；全量 47 通过；npm build 通过

**遇到的问题**
- diagnose 重构时 `return self._run_interactive(...)` 漏 await，M16 恢复测试失败（'coroutine' object has no attribute 'get'）——补 await 后全量恢复绿色

---

## M20 闭环 + 报告：诊断报告导出 + 一键重诊 Before/After 对比（2026-09-23）

**修改内容**
- **A2 报告导出 Word**（`utils/report_docx.py` 新增 + `api/v1/history.py` 扩展）：`POST /history/{task_id}/export-report` 从落库 result 生成三段式报告 docx（综合评分+六维表格 / 差距按维度分组含证据引用 / 改写建议对照+整体建议）；网页态 blob 下载、桌面态 save_path 直写，均落 export_history（template=report）；报告页「导出 Word」一键触发
- **A2 报告导出 PDF**：复用打印方案——`window.print()` 前临时切亮色主题 + body 加 `printing-report` 类，全局打印 CSS 隐藏侧边栏/顶栏/摘要条/Tab 栏，仅打印报告正文；afterprint 恢复主题
- **A3 一键重新诊断**：DB `diagnosis_records` 加 `parent_task_id` 列（走 `_SCHEMA_NEW_COLUMNS` 登记惯例）；`/live/analyze` 接收并透传 `parent_task_id` 落库溯源；SSE payload 与 DB 快照补 `resume_id`/`resume_name`/`parent_task_id` 字段；报告页「⟳ 重新诊断」沿用同一 JD 对当前简历发起新任务并跳转新报告页
- **A3 增量提示**：报告页吸顶摘要条显示「较上次 ±N」（沿 parent 链取父记录综合分做差，成功记录才显示）
- **A3 历史对比视图**：历史页勾选两条成功记录（队列语义，第 3 条自动顶替最早勾选）→「对比」进入对比面板——六维雷达叠加（RadarChart 双系列，Before 灰/After 紫）、综合分 before→after ±N、差距条数变化（负数=改进）、新旧建议左右对照
- **D3 结果页信息架构**：报告页顶部吸顶摘要条（综合分 + 差距数 + 增量 + 页内锚点「评分/优势/差距/建议」+ 右上动作区「导出 Word / 导出 PDF / 重新诊断 / 编辑」）；差距列表按 dimension 分组折叠（LLM 未给维度归「其他」）
- **D4 历史页升级**：单列卡片改紧凑行列表（勾选框 + 状态点 + 岗位/简历名 + 综合分 + 重诊标识 + 时间 + 状态 + 行内查看/删除）；状态筛选（全部/成功/失败/进行中）+ 时间正反排序 + 关键词搜索（前端过滤）；历史列表接口补 `score` 与 `parent_task_id` 字段
- **组件抽取**：六维雷达图从 ResultView 内联 echarts 逻辑抽为 `components/RadarChart.vue`（props: series 多系列 + 主题化取色 + 自动 resize/dispose），结果页与对比页共用
- 测试：`tests/test_report_export.py` 新增 5 个用例（报告 docx 生成与内容、无完整结果 409、列表 score/parent 字段、详情 resume_id、纯函数最小输入）；全量 45 通过；npm build 通过

**遇到的问题**
- 全量 pytest 复跑时 seed 记录撞 `task_id` 唯一约束（tests/.tmp/test.db 跨运行持久化）——seed 改为先 delete 同 id 再 insert 的幂等写法
- SSE 推送 payload 原本不含 resume_id/parent_task_id，前端在 SSE 优先模式下拿不到重诊上下文——stream 端点 payload 补齐三字段（内存 TASKS 与 DB 快照均已携带）

---

## M19 岗位市场：一键获取岗位 + 按简历智能推荐（2026-09-23）

**修改内容**
- **F5 数据源适配层**（`services/job_market.py` 新增）：`JobProvider` 数据源模式——`MockProvider`（12 条真实风格内置样例，无命中自动全量兜底）/ `JSearchProvider`（RapidAPI JSearch，聚合 Indeed/LinkedIn）/ `AdzunaProvider`（免费注册开放数据）；出口统一归一化 `JobItem`（title/company/salary/city/source/jd_text/url）；`JobProviderError` 错误分类（invalid_key/quota/network/unknown）；只接官方/三方数据 API，不爬虫
- **推荐链路**（`api/v1/jobs.py` 新增）：`POST /jobs/search`（关键词+城市检索）；`POST /jobs/recommend`（简历摘要 → Jaccard 关键词重合度粗筛 → LLM 精排打分+一句话理由，JSON 结构化输出 + 池内 id 过滤防幻觉；LLM 不可用自动降级为纯关键词排序，响应标注 degraded）
- **数据源测试**：`POST /settings/test-job` 按错误类型归因（Key 无效/配额用尽/网络不通），与 test-llm 同款体验
- **设置页「招聘数据源」区块**：预设（JSearch/Adzuna/示例数据）+ 控制台直达外链 + Key/App ID 输入 + 测试连接 + 保存（localStorage `job_source_config`，Key 仅存本机）
- **分析页「⚡ 一键获取岗位」面板**：关键词/城市检索；粘贴简历 ≥100 字自动切推荐模式（匹配分+理由）；岗位卡片「用此岗位诊断」一键填入 JD（衔接 F2 复用栈）+ 原文外链；错误归因文案
- 测试：`tests/test_jobs.py` 新增 14 个用例

**遇到的问题与解决**
| 问题 | 解决 |
| --- | --- |
| 计划中的国内数据源核实后不可用：聚合数据「招聘信息大全」官方标注维护中，天行/极速无岗位检索类接口 | 预设改为真实可用的 RapidAPI JSearch 与 Adzuna（免费注册）；适配器模式使数据源可替换不影响架构；Mock 兜底保证演示不落空 |
| recommend 测试真实调用了外部 LLM（服务端预置 Key 生效），慢且不稳定 | monkeypatch 注入 get_llm 故障，专门走降级路径断言 |
| recommend 允许空关键词但继承 search 的 `min_length=1` 导致 422 | 子模型覆写 keyword 允许为空（支持仅按简历匹配） |

**验证**：pytest 全量过（40 用例，含新增 14）；`npm run build` 过。

---

## M18 产品化改造一期：统一壳 + 双主题 + 上手闭环（2026-09-22）

**修改内容**
- **D1 双主题 token**：tailwind 全新语义 token（page/panel/float/inset 四阶底色、line 两阶边框、ink 三阶文本、accent/ok/warn/bad，RGB 三元组支持 `/透明度`）；style.css 亮暗双色板 CSS 变量 + Arco 品牌紫双主题阶；`useTheme`（auto 跟随系统、手动存 localStorage、body arco-theme 联动）；index.html 挂载前定主题防闪屏
- **D0 统一应用壳**：App.vue 重构 ConsoleShell——左侧边栏（介绍/发起诊断/创建简历/简历库占位/诊断历史/设置/更新日志 + 底部主题切换、仓库、版本、配额）+ 窄屏顶栏；路由重组 `/`=纯介绍、`/app/*`=工作台、旧路径 redirect；桌面启动直进 `/app/analyze`
- **A1+D2+F2 分析页重排**：双栏布局、上传/粘贴双 Tab、示例一键填入（前端内置 `data/example.js`）、JD 最近 5 条复用（localStorage）；后端新增 `POST /resumes/upload-text`（ResumeService.save_text）
- **A5 Key 引导**：`useKeyGuide` 全局状态——无 Key 首启弹三步引导层（可稍后再说）、侧边栏设置项小红点、分析页提交前拦截
- **A6 模型预设增强**：厂商预设 3 → 10 家（新增通义千问/Kimi/豆包/硅基流动/OpenRouter/Ollama 本地/自定义），推荐模型列表 + 控制台直达链接 + 向量检索支持标注；`test-llm` 错误按类型归因（Key 无效/余额不足/限流/URL 或模型名/网络）附原始错误
- **D5 组件化**：`EmptyState`/`LoadingBlock` 全局组件；Result/Chat/History/Editor/Changelog/ResumePreview 全站 token 化扫荡（拟物阴影/裸色值清零、路由字符串 /app 前缀、空态组件化）
- 测试：新增 `test_upload_resume_text`（含 50 字下限校验）

**遇到的问题与解决**
| 问题 | 解决 |
| --- | --- |
| Tailwind 语义色需支持透明度修饰符（bg-accent/10），直接用 `var(--hex)` 不行 | CSS 变量存 RGB 三元组，配置 `rgb(var(--c-x) / <alpha-value>)` |
| 桌面端「直进工作台」若用常驻 watch 会把用户主动回介绍页也弹回去 | 只在启动后首次判定时 redirect 一次（redirected 标记），不劫持后续导航 |
| 主题切换在 SPA 挂载后才执行会闪屏（首帧用错主题） | index.html 内联脚本在应用加载前写 `data-theme`，useTheme 只负责运行时切换 |
| 旧书签/旧 exe 深链（#/result/xxx）在路由重组后 404 | 旧路径全部登记 redirect（含参数透传） |

**验证**：pytest 全量过（8 文件，含新增用例）；`npm run build` 过；亮/暗双主题人工走查。

---

## M17 Web 形态压力摸底（2026-09-22）

**修改内容**
- 新增 `backend/scripts/stress_web.py` 压测脚本：进程内 uvicorn（port=0 随机端口）+ mock LLM 每节点延时 8s 拉长任务窗口；① 单诊断任务上阶梯开 25/50/100/150 条 SSE 连接（每波保持 7s），测首包延迟与进程内存 ② 20/60/120 工作循环并发插入 diagnosis_records（模拟进度节流写的极端化），测吞吐与锁错误；数据全部落 `tests/.tmp/stress/`
- 结论写入技术说明文档 13.4：SSE 150 并发全连上（首包 p50 0.35s / p95 0.47s / 0 错误 / 218MB，约 +0.6MB/连接）；WAL 写吞吐 ~350-400 ops/s（瓶颈在连接池 5+10 排队），busy_timeout=5000 下偶发 locked 约 0.03%

**遇到的问题与解决**
| 问题 | 解决 |
| --- | --- |
| `logging.getLogger("sqlalchemy.engine").setLevel(WARNING)` 放在 app 导入前不生效 | engine `echo=True` 在创建时强制重设 INFO——日志抑制必须放在 `app.core.db` 导入**之后** |
| ctypes `GetProcessMemoryInfo` 恒失败（内存读 -1） | 64 位下 `GetCurrentProcess()` 伪句柄被 ctypes 按 32 位 int 截断，需 `restype = ctypes.c_void_p` 后再传入 |
| SSE 推送只在 payload 变化时发包，无逐秒心跳 | 改测「首包延迟 + 事件数」而非心跳间隔，忠实反映 fan-out 响应能力 |
| 客户端与服务端同进程，GIL 竞争使延迟偏大 | 作为下界参考如实注明测法局限（真实部署为独立进程，结果只会更好） |

**验证**：脚本两次运行结果一致可复现；未触碰运行时 data/ 目录。

---

## M16 跨重启恢复 + 草稿暂存（2026-09-21）

**修改内容**
- **SqliteSaver 持久化**：交互诊断图 checkpointer 由 MemorySaver 升级为 SqliteSaver（`data/checkpoints.db`，进程级单例 + `check_same_thread=False`），`interrupt()` 暂停时的图状态落文件快照，重启不丢；快照库初始化失败自动降级 MemorySaver
- **waiting 任务跨重启恢复**：`diagnosis_records` 新增 `questions` / `clarify_ctx` JSON 列（老库自动补列），进入 waiting_clarify 时问题列表与恢复上下文（jd_text / city / llm_config / enable_refine / resume_name / resume_id）落库；`resume()` 内存无条目时自动从 DB 重建；启动 lifespan 补偿扫描 waiting 记录重建进内存表；无上下文的老版本遗留任务优雅标记 failed 而非永远悬置
- **前端追问草稿暂存**：追问回答实时存 `localStorage clarify_draft_{taskId}`，误关页面重开不丢已填内容，提交成功后清除
- 打包 hiddenimports 补 `langgraph.checkpoint.sqlite` / `sqlite_vec`；新增 `test_live_recovery.py` 3 项集成测试

**遇到的问题与解决**
| 问题 | 解决 |
| --- | --- |
| `_default_db_file()` 首版引用了不存在的 `app_settings.db_url`，且调用位于降级 try 块内——ImportError 被静默吞掉，SqliteSaver 永远降级 MemorySaver，功能整体静默失效 | 修正为 `settings.DB_URL`；教训：**降级兜底的 try 块会把实现错误伪装成环境问题**，依赖降级的路径必须有测试或日志暴露 |
| SqliteSaver 快照库（tests/.tmp/checkpoints.db）跨 pytest 运行留存，`test_interactive_clarify_pause_and_resume` 同 thread_id 重跑时合并上次已完成运行的 channel 残留（suggestions 非空）导致断言失败 | conftest 启动时清理 `checkpoints.db*` 残留（MemorySaver 时代进程退出即清空，无此问题） |
| `.tmp` 测试库跨运行留存，legacy 用例固定 task_id `legacy-wait` 触发 UNIQUE 冲突 | 用例改 `uuid4().hex` 随机 id |
| 测试里 `LivePipelineService.TASKS` 报 AttributeError | 内存任务表是模块级 dict（`live_pipeline_service.TASKS`）而非类属性 |
| 计划中的 scorer 并行化优化 | 核实后**不做**：scorer 是单次 LLM 调用评六维，证据检索纯 CPU 毫秒级——无并行收益，属伪优化 |

**验证**：pytest 25 过（22+3，含模拟重启后 Command(resume) 从 DB 重建续跑成功的端到端断言）；`npm run build` 过。

---

## M15 独立简历创建 + 仓库入口（2026-09-21）

**修改内容**
- **独立创建简历模式**：编辑器支持无任务进入——新增路由 `/editor`（原 `/editor/:taskId` 保留），首页新增「✍️ 免费创建简历」入口；空白模式（taskId 为空）不拉取任何任务数据，编辑内容自动存 `resume_edit_blank`（localStorage），与诊断链路的存档键隔离互不污染；顶栏标题区分「创建简历 / 简历编辑」，无任务时隐藏「← 返回报告」
- **开源仓库跳转**：首页 CTA 区新增 GitHub / Gitee 链接（官方 SVG 图标，新窗口打开），地址与 `app/__init__.py` 元数据一致
- **测试素材**：`backend/tests/fixtures/` 新增两份 docx 简历（Java 后端 3 年高匹配 / 前端转后端低匹配）+ 一份共用 JD 文本，已通过 `parse_resume_file` 冒烟，用于 A/B 对照诊断与消融复跑

**遇到的问题与解决**
| 问题 | 解决 |
| --- | --- |
| 编辑器 `taskId` 此前为必选路由参数，存档键直接拼 taskId，空值会把 key 变成 `resume_edit_undefined` | `taskId = route.params.taskId || ''`，键名统一 `_${taskId || 'blank'}`；`loadData()` 在空白模式跳过 sessionStorage/后端三层回退 |
| 编辑器模板「添加」按钮依赖区块 v-for，教育/经历初始空数组时无入口 | 已确认模板自带「+ 添加教育经历 / + 添加工作经历 / + 添加项目」与空态提示，空白起步可完整填写 |

**验证**：`npm run build` 过；docx 解析冒烟过（819/597 字）；`/editor` 页面待下次打开应用时人工确认。

---

## M14 导出体验升级（2026-09-21）

**修改内容**
- 桌面端 Word 导出改为**原生「另存为」对话框**选保存路径（`desktop.py` 新增 `DesktopApi` js_api 桥），服务端直写所选路径，保存位置明确可见；网页形态保持浏览器下载
- 新增**导出历史**：`export_history` 表 + `GET/DELETE /resumes/export-history` 接口；编辑器顶栏「导出历史」弹层可查看最近 30 条（时间/文件名/保存路径），桌面态可一键「打开位置」（资源管理器选中文件），网页态标注「浏览器下载」
- **PDF 导出修复**：打印从 ResumePreview HTML 模板改为**直印 docx 预览 DOM**——打印输出、屏幕预览、Word 导出三者版式完全一致（此前打印用的是另一套 HTML 模板，与 Word 排版存在偏差）

**遇到的问题与解决**
| 问题 | 解决 |
| --- | --- |
| 桌面 webview 里 blob 下载落进系统下载目录，用户不知道文件在哪 | pywebview `create_file_dialog(SAVE_DIALOG)` 选路径 → `export-docx` 带 `save_path` 服务端直写；后端校验仅桌面形态可用（网页形态禁写用户任意路径）、绝对路径、.docx 后缀、父目录存在 |
| pywebview `create_file_dialog` 返回值版本差异（str / list） | 兼容处理：list/tuple 取首个，空返回 None 视为用户取消 |
| 打印内容与 Word 导出版式不一致（旧问题 I-01 的打印残余） | `downloadPdf()` 把 docx-preview 渲染的 DOM 快照注入打印区 `v-html`，打印后清理；CSS 增加 `.docx-wrapper` 去灰底/去阴影适配；预览未就绪时回落 ResumePreview |

**验证**：pytest 22 过；`npm run build` 过；桌面态待重新打包后实测对话框与「打开位置」。

---

## M13 健壮性优化 + 文档收敛（2026-09-21）

**修改内容**：同 owner 重复诊断 409 防护（`has_active_task`）；追问回答单条截断 500 字；`models/schemas.py`（仅剩 HealthOut）并入 `health.py` 删除；docs/ 收敛为三件套（计划 / 技术说明 / 论文准备），原「问题解决记录」「评估报告」「消融评估报告」并入技术说明第 12/13 章。

**遇到的问题与解决**
| 问题 | 解决 |
| --- | --- |
| 文档散乱：7 个 md 互相重叠、部分内容停留在 M10 | 明确三件套分工（计划=迭代史、技术说明=实现+踩坑+评估、论文准备=知识点+答辩），全仓库死链逐一修正 |

**验证**：pytest 22 过。

---

## M12 检索质量 + 防幻觉加固（2026-09-21）

**修改内容**：evidence.py 检索打分升级 BM25-lite（IDF 加权 × √查询覆盖度 ÷ √块长度）；gap/rewriter prompt 增加空证据池防幻觉指令。

**遇到的问题与解决**
| 问题 | 解决 |
| --- | --- |
| 「负责/项目」等高频通用词与 Kubernetes 等区分词同权，检索排序不准 | BM25-lite 打分：IDF=`log(1+N/df)`，DF 表缓存在索引上每任务复用 |
| 消融实验发现空池下模型仍幻觉出恰好合法的证据 id（如 R1） | prompt 明令空池留空 + `filter_valid_ids` 校验双保险；消融 B 变体需「检索+校验双禁」才构成有效消融 |

**验证**：pytest 22 过；真实 LLM E2E 引用率 5/6 → 6/6；消融结论跨运行可复现（RAG +8 分）。

---

## M11 RAG 证据接地 + 动态多轮追问（2026-09-21）

**修改内容**：新增 `core/evidence.py`（分块/索引/检索/校验四步流水线，embedding 不可用自动降级 IDF 关键词检索）；scorer/gap/rewriter 三节点证据接地；LangGraph `clarify_plan/clarify_wait` 双节点 + `interrupt()` 人在回路动态追问（0-3 问）；`POST /live/clarify` 暂停恢复链路；前端追问卡片 + 差距条目 `[R*]` 证据引用展示；`scripts/evaluate.py` 稳定性+消融评估。

**遇到的问题与解决**
| 问题 | 解决 |
| --- | --- |
| Python 3.10 下 async 图 `interrupt()` 失效（contextvars 行为 3.11+ 才正确） | 官方兼容解：交互图同步节点包装 + worker 线程驱动同步 stream；离线图保持 async |
| clarify_plan 恢复时重复 LLM 调用 | 节点**先提交 state 再 interrupt**，恢复时跳过规划直接读 state |
| 空池下 LLM 编造证据 id | 防幻觉四层体系（见技术说明 6.8.3） |
| 评估脚本首次运行 ImportError | 修复模块导入路径后重跑 |

**验证**：22 项 pytest（含暂停/恢复集成测试）；真实 LLM E2E（glm-4-flash）追问→回答→续跑全链路通过；消融：RAG 贡献 +8 分、引用率 100% vs 0%。

---

## M10 清理盘点 + 预览保真（2026-09-21）

**修改内容**：清理 10 个可再生缓存目录；死代码三项 grep 验证均有引用保留；EditorView 预览改为调用 export-docx 生成真实 Word + docx-preview 直渲（预览=导出），`buildWordData()` 与下载共用；技术说明文档 M1-M10 表述修正。

**遇到的问题与解决**
| 问题 | 解决 |
| --- | --- |
| 预览（HTML 模板）与 Word 导出像素级差异 | 预览直接渲染导出文件本身（docx-preview），600ms 防抖 + seq 序号丢弃过期响应 |

**验证**：npm build 过；export-docx 冒烟 200；浏览器实测渲染正常。

---

## M9 大清理（2026-09-21）

回收 1.8GB：删除 clarifier_agent.py、run_diagnosis_test.py、过时文档、测试残留；`.idea` 取消跟踪；`build_desktop.py` 增加 `_prune_old_artifacts()`（release/ 只留 1 版回退）。

## M8 双形态界面 + 启动提速（2026-09-21）

- `/api/v1/meta` 下发运行形态，前端 `useAppMode` 适配桌面紧凑风/网页引流界面；首页重写
- **启动 52.6s → 2.09s**：本机对关闭端口 TCP connect 会静默挂到超时，先 `SO_EXCLUSIVEADDRUSE` bind 探测短路，只有被占端口才发 HTTP 验身份

## M7 exe 串台修复 + 原生窗口（2026-09-21）

- `SO_REUSEADDR` 在 Windows 会误判他人已占端口为空闲（串台蓝笔申论）→ 改 `SO_EXCLUSIVEADDRUSE`
- 单实例锁只认 `/_sig` ResuMatch 签名响应，不误伤其它应用；pywebview 原生窗口，回退浏览器；windowed 模式日志落 `data/logs/app.log`

## M6 SSE 实时诊断 + 落库 + 评估（2026-09-21）

LangGraph 六节点诊断链（parser ∥ job_analyze → scorer → gap → rewriter → refine）+ SSE 逐节点推送 + DB 落库（0.8s 节流）+ evaluate.py 评估脚本 + 截图。**问题**：并行节点互相覆盖 error → 自定义 reducer `_merge_error`。

## M1-M5 基础建设（2026-09-20 及以前）

M1 FastAPI+Vue3 脚手架；M2 SQLite+SQLAlchemy 2.0（StaticPool 并发错乱 → AsyncAdaptedQueuePool+WAL）；M3 简历上传解析（PDF/docx）；M4 多模板 Word 导出（中文 eastAsia 字体回落）；M5 桌面打包（PyInstaller 排除清单、photo_id 路径穿越防护、DeepSeek 不支持 tool calling → 手写三层容错）。详见技术说明文档。

---

*格式约定：每个迭代一节，含「修改内容 / 遇到的问题与解决 / 验证」三部分；问题表只记仍然成立的技术经验。*
