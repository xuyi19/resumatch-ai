# ResuMatch AI 改造计划

> 目标：去爬取、去 MySQL，以「桌面 exe + 可上线网站」双形态交付；修复使用逻辑；补充简历模板与证件照能力。
> 每完成一项打勾并注明日期。

## M36 工作台总览仪表盘（2026-09-24）

- [x] **后端**：GET /history/stats 聚合端点（简历/诊断/面试统计 + 进行中明细；注册须在 /{task_id} 之前）
- [x] **前端**：DashboardView 设为 /app 默认页——统计卡 / 继续进行 / 最近记录双列 / 空态引导 / 失败可重试
- [x] **导航**：侧边栏「总览」入口 + 404 兜底改指 dashboard
- [x] **测试**：test_dashboard_stats.py（test.db 残留数据用「清理固定标识 + 增量断言」模式）

## M35 差距条目标记「已解决」（2026-09-24，补产品化计划漏项 F4）

- [x] **报告页标记**：ResultView 差距条目点击 △→✓（划线+[已解决]），localStorage `gap_done_{task_id}` 持久化（key=dimension|description）
- [x] **重诊对比联动**：HistoryView 对比视图新增「差距对照」区（Before/After 明细），Before 已标记项绿色高亮 + 顶部「已解决 x/y」统计
- [x] localStorage 读写全程 try/catch 兜底（损坏值/隐私模式不白屏）

## M34 面试评分量化（2026-09-24）

- [x] **schema**：InterviewSummary 加 overall_score + scores（旧会话缺省自动隐藏）
- [x] **前端**：总分档位色大数字、RadarChart 参数化四类雷达、题单卡/题干消息得分徽标、会话列表总分
- [x] **导出报告**：每题得分 + 总分
- [x] **测试**：fake 与断言补分数；pytest 63 过

## M33 面试报告导出 Word（2026-09-24）

- [x] **docx 生成**：`generate_interview_docx`（问答记录 + 总评，复用诊断报告样式基建）
- [x] **端点**：`POST /interview/export/{task_id}`（blob / save_path 直写双模式，落 export_history template=interview）
- [x] **前端**：总评区「📄 导出面试报告」按钮（blob 下载）
- [x] **测试**：409（未完成）/ 404（不存在）/ docx 魔数与 content-type

## M32 面试官多轮自由对话（2026-09-24）

- [x] **多轮自由对话**：`Conversation.chat_log` TEXT 列存完整对话流；新端点 `POST /interview/chat/{task_id}`——候选人围绕当前题随时发言，LLM `ChatTurn{reply, advance}` 自然回应（可追问/可补充），`advance` 收尾时本题发言合并落 `answers` 并推进下一题，末题出总评；`force_advance` 强制收尾支持「本题回答完毕」按钮；旧 mock 逐题端点删除，旧数据由 `_chat_base_log()` 从 answers 合成对话流（TTS 曾实现后按用户要求移除）
- [x] **聊天式 UI**：InterviewPanel 模拟面试区重构为消息流（面试官/候选人气泡、题干高亮、题号分类标签）+ Enter 发送 + 「本题回答完毕，进入下一题 →」+ 自动滚动 + 思考中指示
- [x] 验证：pytest 61 通过（chat 全流程/校验/透传 3 例重写）；npm build 通过

## M31 模拟面试独立成页 + 面试面板组件化（2026-09-24）

- [x] **独立面试入口**：侧边栏新增「模拟面试」页（`/app/interview`）——不跑诊断也能开面试：选简历 + 粘贴意向岗位 JD → `POST /interview/start-free` 创建独立会话（`iv-` 前缀 task_id，上下文存 `Conversation.context` 新 TEXT 列）直接出题
- [x] **会话管理**：`GET /interview/sessions` 最近 20 场列表（独立/诊断来源、进度、是否已出总评），点卡片继续面试；`?task_id=` query 直达恢复；「重新出题」加 regenerate 参数真正生效
- [x] **上下文双模式**：`_load_context` 优先诊断任务（DiagnosisRecord），回退独立会话（context JSON）——出题/答题/总评端点零改动兼容两种来源
- [x] **面试面板组件化**：抽取 `InterviewPanel.vue`（准备/模拟双子区）；诊断结果页 Tab 4 → 3（诊断报告/优化简历/面试），内联模板与脚本整体迁出
- [x] 验证：pytest 59 通过（新增 test_interview_free.py 6 例含 llm_config=null 回归）；npm build 通过；浏览器 E2E 全链路通过（出题→答题→点评→进度 2/6→会话列表恢复）

## M30 数据源精简 + 手动导入公司比对（2026-09-24）

- [x] **删除海外数据源**：JSearchProvider / AdzunaProvider 整体移除（不覆盖国内岗位，实际无用）；PROVIDER_IDS 只剩 ai + mock；旧 localStorage 配置（jsearch/adzuna）自动迁移到 AI 生成；相关测试同步清理
- [x] **手动导入公司比对**：新端点 `POST /jobs/import-match`（resume_text + items[]{company,title,city,salary,jd} ≤20 条 → 粗筛 + LLM 精排，JD 缺失用公司+岗位占位）；分析页岗位面板拆「搜岗位 / 导入比对」双 Tab，导入列表 localStorage 持久化、chip 可删、与简历比对出匹配分排序，结果卡片可一键回填 JD
- [x] **重构**：recommend 的粗筛+LLM 精排块抽取为 `_rank_for_resume()`，recommend 与 import-match 共用
- [x] 验证：pytest 54 通过（test_jobs 改造 + 新增 2 例）；npm build 通过；浏览器 E2E 全链路通过（真实 LLM 精排 90/60 分排序正确）

## M29 诊断卡死修复 + 国内岗位方案（2026-09-23）

- [x] **诊断卡死根因**：waiting_clarify 任务启动时恢复进内存并永久占用「进行中」名额，追问界面无放弃出口 → 409 死锁
- [x] **放弃任务闭环**：`POST /live/abandon/{id}`（内存+DB 双清，abandoned 标记阻断 run() 复活）；追问屏加「放弃此任务」；409 响应带 `X-Active-Task` 头，前端弹窗引导「前往该任务」
- [x] **24h 自动作废**：发起诊断时清理超过 24h 未应答的 waiting_clarify（内存+DB）
- [x] **国内岗位换方案**：原 JSearch/Adzuna 只覆盖海外岗位——新增 `AIJobProvider`（ai）：用用户已配置模型生成目标岗位「参考画像」（人民币薪资/国内公司），无需额外 Key，失败降级示例数据；JSearch/Adzuna 保留并标注「不含国内」
- [x] 验证：pytest 56 通过（新增 4 例）；npm build 通过；实测旧卡死任务已放弃释放名额、AI 源真实模型返回 10 条国内岗位

## M28 更新日志补全 + 版本号统一（2026-09-23）

- [x] **版本单一事实源**：`APP_VERSION` 默认值引用 `__version__`（0.9.0），`.env` 删除覆盖行；package.json 同步
- [x] **站内更新日志页**：补 v0.6.0-v0.9.0 四档（覆盖 M1-M27 全部用户可感知变化）；页头「当前版本」徽标 + 最新条目绿标
- [x] 验证：pytest 52 通过；npm build 通过；浏览器实测徽标/时间线/侧边栏版本号正常

## M27 简历库 PDF 预览（2026-09-23）

- [x] **原文件保留**：上传时 pdf/docx 原件落盘 `data/files/{resume_id}{ext}`（Resume.file_path 列，老库自动补列；落盘失败不阻塞入库）；txt 粘贴简历不存原件
- [x] **预览端点**：`GET /resumes/{id}/file`（inline disposition 供 iframe 直渲；下载名跟随改名）；`/list` 返回 `file_type`
- [x] **前端预览升级**：PDF 弹层内浏览器原生渲染（保真版式）+「下载原文件」；docx/纯文本仍文本预览；卡片加文件类型徽标
- [x] **删除闭环**：单删/批量删除/一键清空均连带清理落盘原件（按 file_path 精确删，web 多用户安全）
- [x] 验证：pytest 52 通过（新增 4 例）；npm build 通过；浏览器实测弹层渲染/下载/回退

## M26 去重收尾（2026-09-23）

- [x] **重复上传拦截**（上轮后端 `find_duplicate` / `duplicated` 标志）：AnalyzeView 粘贴命中去重时提示「已直接复用（未新建记录）」
- [x] **run.py 关闭体验重做**：独立浏览器实例（关浏览器=自动停服）+ Job Object 进程树兜底（关主窗口/Ctrl+C 系统级回收，无残留）；服务窗口降噪（SQL echo 与 DEBUG 解耦、vite 仅告警级）
- [x] **简历库批量删除**：后端 `POST /resumes/delete-batch`；前端卡片勾选 + 「删除选中（N）」二次确认
- [x] 验证：pytest 48 通过；npm build 通过

## M25 F1/F3 收尾：编辑器入库 + 简历快速体检（2026-09-23）

- [x] **F1 链路补全**：EditorView 新增「⭐ 保存到简历库」——结构化编辑数据拼纯文本（`buildPlainText`）走 `uploadResumeText` 入库，创建→保存→简历库→一键诊断全链路闭环
- [x] **F3 简历快速体检**：新增 `utils/checker.js` 纯前端规则检查（缺姓名/联系方式/教育/经历=硬伤，弱动词开头/全无量化数字/描述过短/总结缺失=建议）；编辑器左栏顶部实时体检卡（随编辑即时刷新，硬伤计数）
- [x] 验证：npm build 通过（本次纯前端改动，pytest 无涉及）
- 至此产品化完善计划 A/B/C/D/F **全部项目落地完毕**

## M24 F1 简历库：独立管理页（2026-09-23）

- [x] **后端**（`api/v1/resume.py`）：`GET /resumes/list`（列表 + 各简历最近一次成功诊断综合分）、`POST /resumes/rename/{id}`、`DELETE /resumes/{id}`；`get_resume` 补 `raw_text` 供预览
- [x] **前端**：新增 `ResumeListView.vue`（卡片列表：名称/时间/字数/最近诊断分，操作：发起诊断/预览弹层/重命名/删除，EmptyState/LoadingBlock 统一空态）；路由 `/app/resumes`；侧边栏「简历库」入口从占位转正式；AnalyzeView 支持 `?resume_id=` 预选（提示条 + 跳过重复上传，创建→保存→从简历库诊断链路闭环）
- [x] 验证：pytest 48 通过；npm build 通过

## M23 面试功能落地：面试准备 + 模拟面试（2026-09-23）

> 消灭产品化计划第 15 环节空壳（报告页两个「即将上线」Tab）——最后一块功能空白。

- [x] **面试准备**：`POST /interview/start/{task_id}` 基于简历+JD+诊断差距生成 6 题（技术基础/项目深挖/岗位匹配/情景行为，附考察点+回答思路），存 Conversation(type=interview)；前端题单卡片 + hint 折叠 + 重新出题
- [x] **模拟面试**：`POST /interview/mock/{task_id}` 多轮对话——逐题回答 → LLM 点评（先肯定后建议）→ 答完出总评（overall/strengths/weaknesses/suggestions）；会话可恢复（GET /interview/{task_id}）；前端逐题轮次卡 UI + 进度 + 总评卡
- [x] 复用 M17 Conversation 表（type=interview，M17 设计时预留）；llm_config 透传
- [x] 验证：pytest 48 通过；npm build 通过

## M22 收尾：数据信任 + 更新检查 + 体验拾遗 ✅（2026-09-23）

> 依据 [`产品化完善计划.md`](产品化完善计划.md) C1 / C2 / C3，产品化完善计划全部实施完毕。

- [x] **C1 隐私说明 + 清空数据**：设置页「数据与隐私」区块（本机存储说明 + LLM 服务商告知）；`DELETE /api/v1/data` 一键清空本用户全部数据（诊断记录/简历/导出历史/会话/证件照/graph 快照，web 多用户按 owner 隔离不误删），前端二次确认
- [x] **C2 exe 更新检查**：`desktop.py` 启动后台线程请求仓库 VERSION（Gitee/GitHub 双源，5s 超时静默失败），有新版 webview 原生弹窗提示；仓库根新增 VERSION 文件（当前 0.5.0）
- [x] **C3 体验拾遗**：追问卡片逐条校验提示（指出具体哪几题空着）；HistoryView 删除已随 D4 完成；窄屏适配随 M18 token 重做覆盖
- [x] 测试：test_clear_data.py（清空 + 幂等），全量 48 通过；npm build 通过

## M21 容错 + 启动体验：失败重试 + 扫描件引导 + 一键启动降噪 ✅（2026-09-23）

> 依据 [`产品化完善计划.md`](产品化完善计划.md) B1 / B2，附一键启动脚本优化。

- [x] **B1 失败保留部分结果**：`diagnosis_service.diagnose` 交互模式异常时从 checkpointer 快照恢复已完成节点结果，抛 `DiagnosisPartialError`；`_flow` 捕获后 partial result 与 error 并存落库（result.partial=true 标记），前端失败态显示「已产出：六维评分 ✓ / 差距 ✓」
- [x] **B1 同任务重试**：`POST /live/retry/{task_id}`——校验失败态 + 快照存在后重置任务态，`resume_after_error`（graph input=None）从断点续跑，已完成节点不重跑（不重复消耗 API 费用）；失败任务不再释放 checkpointer 快照（成功才释放）；前端失败态「🔁 从失败处继续」留在报告页重新接入 SSE
- [x] **B2 扫描版 PDF 明确引导**：`file_parser.parse_pdf` 返回 (text, image_only_pages, total_pages)，整本几乎无文本且主要图片页时抛「检测到扫描版/图片型 PDF，无法提取文字，请换文字版或先 OCR」文案
- [x] **run.py 一键启动优化**：去掉内网穿透/MySQL 残留；后端 `--no-access-log`、前端 vite `--logLevel warn` 降噪；主窗口 ANSI 配色紧凑横幅 + 关键 URL
- [x] 测试：test_file_parser.py 2 用例（扫描版报错文案 / 文字版正常），全量 47 通过；npm build 通过

## M20 闭环 + 报告：报告导出 + 重诊对比 ✅（2026-09-23）

> 依据 [`产品化完善计划.md`](产品化完善计划.md) A2 / A3 / D3 / D4，实施于 M19 之后。

- [x] **A2 报告导出 Word**：`utils/report_docx.py` + `POST /history/{task_id}/export-report`——评分表格+差距+建议三段式 docx，落 export_history（template=report），网页 blob 下载 / 桌面 save_path 直写
- [x] **A2 报告导出 PDF**：window.print() + `printing-report` 打印 CSS（隐藏侧边栏/顶栏/摘要条/Tab），打印前临时切亮色主题
- [x] **A3 一键重新诊断**：`diagnosis_records.parent_task_id`（`_SCHEMA_NEW_COLUMNS` 登记）；`/live/analyze` 透传落库；报告页「⟳ 重新诊断」沿用同 JD 重诊当前简历
- [x] **A3 增量提示**：摘要条「较上次 ±N」（parent 链取父记录综合分做差）
- [x] **A3 Before/After 对比**：历史页勾选两条 → 雷达叠加（双系列）+ 综合分/差距条数变化 + 新旧建议对照
- [x] **D3 结果页信息架构**：吸顶摘要条（综合分+差距数+增量+锚点+动作区）+ 差距按维度分组折叠
- [x] **D4 历史页升级**：紧凑行列表（含综合分/重诊标识）+ 状态筛选 + 时间排序 + 关键词搜索 + 行内查看/删除；列表接口补 score/parent_task_id
- [x] **组件抽取**：`components/RadarChart.vue` 多系列雷达（结果页/对比页共用）
- [x] 测试：test_report_export.py 5 用例，全量 45 通过；npm build 通过

## M1 清理去爬虫 ✅（2026-09-18）

- [x] 删除 `app/crawlers/`（Playwright 抓取）与相关依赖
- [x] `/live/analyze` 改为只接收用户粘贴的 JD 文本（`jd_text`）
- [x] 数据库从 MySQL 迁移到内置 SQLite（aiosqlite + WAL），删除 MySQL 驱动依赖
- [x] `run.py` 重写：`sys.executable -m uvicorn` + find_spec 依赖预检，去掉 MySQL/cpolar 残留
- [x] 城市字段从前端移除（后端保留可选兼容）
- [x] 文档残留清理（历史文档顶部标注为历史版本）

## M2 修复使用逻辑 ✅（2026-09-18）

- [x] `resume_id` 落库：诊断记录与原始简历关联（chat/optimize 复用原文不再依赖内存）
- [x] 任务创建即落库（`create_task` 携带 resume_id / resume_name / owner_id）
- [x] 启动补偿：残留 running 记录标记失败（cutoff 用 naive UTC 比较）
- [x] `keyword` 改为诊断完成后回填岗位名（取 job_analysis.summary 前 24 字）
- [x] chat 空简历报错明确化（未关联原始简历时提示重新诊断）
- [x] JD 全文持久化进 result，供 chat / optimize 阶段复用
- [x] 内存任务表 TTL 惰性清理

## M3 桌面 exe 强化 ✅（2026-09-18）

- [x] pywebview 窗口化（默认原生窗口 + 浏览器回退 + `--no-browser` / `--browser`）
- [x] 单实例锁：只认 ResuMatch 健康响应，不误伤其它应用
- [x] PyInstaller 打包排除 MySQL 驱动 / torch / Chromium；build_desktop.py 自带 PATH 前置与 DLL 自校验
- [x] exe 烟测通过（启动、建表、静态页、API、零配置 Key）
- [x] README 重写 + docs/images/ 真实截图 5 张

## M4 网站化 + 模板与照片 ✅（2026-09-18）

- [x] **owner 会话隔离**：三业务表（resumes / diagnosis_records / conversations）加 `owner_id` 列
  - `APP_MODE=local`（默认）：固定 owner=local，行为同单机
  - `APP_MODE=web`：HttpOnly cookie（`rmsid`）匿名会话，支持 `X-Session-Id` 头直调；`app/core/deps.py::get_owner_id`
  - 老库升级：`ensure_schema_columns()` PRAGMA 检查 + ALTER TABLE 补列 + 回填 local（conftest 共用）
- [x] 全部读写端点注入 owner 过滤：resume / live / history / chat / optimize
- [x] **Web 每日配额**：服务端 Key 计 `WEB_DAILY_LIMIT`（默认 10 次/天，UTC 日界），自带 Key 不限，超限 429
- [x] **10 套简历模板**（docx_generator）：classic / sidebar / business / elegant / modern / minimal / academic / creative / twocol / compact；`GET /resumes/templates` 下发目录
- [x] **证件照**：`POST/GET/DELETE /resumes/photo`（魔数验证 JPG/PNG、≤5MB、uuid 文件名防穿越）；`export-docx` 支持 `photo_id` 嵌入一寸照位
- [x] 前端编辑器：模板下拉（后端目录）、照片上传预览、导出带 template + photo_id；预览区照片示意 + 打印导出透传
- [x] 前端 AnalyzeView 移除 URL 抓取方式与城市选择；HistoryView 移除 city 展示
- [x] 测试：14 项 pytest 全过（含模板目录、照片上传导出、web 态 owner 隔离）

## M6 实时性 + 收尾 + 质量评估 ✅（2026-09-18）

- [x] **SSE 流式进度推送**：`GET /live/stream/{task_id}` 推送诊断进度（1s 轮询快照去重后下发，终态关流），前端 EventSource 替代轮询，连接中断自动回退 2s 轮询（`/live/status` 保留兼容）
- [x] **任务状态落库**：进度（stage/progress/logs）0.8s 节流写入 diagnosis_records（老库经 `_SCHEMA_NEW_COLUMNS` 补列），`/live/status` 与 SSE 在内存任务过期/重启后回退纯 DB 读取（含 owner 校验），多进程部署就绪
- [x] **self-refine 改写循环**：rewriter 后追加 refine 节点（自我批判：忠实原文/紧扣 JD/STAR/去重，失败保留原建议不熔断）；`ENABLE_SELF_REFINE` 配置开关默认开，请求体 `enable_refine` 可覆盖；前端进度条新增「精修优化」阶段（开关关闭时自动隐藏）
- [x] **语义匹配形态评估**：决策——不重新引入 embedding 服务（app/matching 已在 M1 移除，torch/sentence-transformers 会使 exe 膨胀 1GB+）；LLM 链路（job_analyze/gap）本身即语义匹配主通道，评估脚本内置轻量词面重叠指标（JD 关键词命中率）作量化补充
- [x] **质量评估脚本**：`backend/scripts/evaluate.py` 内置 3 组简历-JD 标注集（高/中/低匹配），多次运行统计 overall 均值/标准差与改写覆盖度（建议数/量化占比/JD 关键词命中），产出评估报告（M13 起并入技术说明文档第 13 章）
- [x] **全屏重拍 README 截图**：5 张全屏界面图替换 docs/images/（result.png 用历史真实报告任务渲染）
- [x] pytest 15 过（新增 enable_refine=False 用例）+ npm run build 过 + SSE/落库端到端冒烟通过 + exe 烟测

## M7 桌面版体验修复（2026-09-18）

- [x] **端口串台修复**：`desktop.py` 端口探测由 `SO_REUSEADDR` 改为 `SO_EXCLUSIVEADDRUSE`——Windows 下前者允许“绑定”他人已占端口，导致 8765 被其它应用占用时误判空闲，浏览器打开别人应用的页面
- [x] **原生窗口恢复**：pywebview（Edge WebView2）原生窗口，异常自动回退浏览器；`--browser` / `--no-browser` 保留；`webview.start()` 修正为必须在主线程调用
- [x] **无控制台打包**：PyInstaller 加 `--windowed`，stdout/stderr 为空时垫 devnull 并将日志写入 `data/logs/app.log`
- [x] **单实例锁修复**：改为扫描 8765+50 区间寻找 ResuMatch 健康响应（原硬编码 8765，端口顺延时漏判会重复启动）
- [x] **打包补齐**：`--collect-all webview` + edgechromium/pythonnet 隐藏导入，排除 Qt/GTK/CEF 等其它平台后端；requirements.txt 移除已废弃的 playwright，补 pywebview/pyinstaller
- [x] exe 烟测：健康检查 / 静态页 / 10 套模板接口 / 原生窗口标题 / 日志落盘 全部通过

## M8 启动性能 + 双形态界面 + 首页重写（2026-09-18）

- [x] **exe 启动提速 25 倍（52.6s → 2.1s）**：根因是 `_find_running_port()` 对 8765+50 区间逐个发 HTTP 健康检查，而本机对「已关闭端口」做 TCP 连接不会立刻拒绝、会静默挂满 1 秒超时 → 全空闲时白等 50.85 秒。改为先用 bind 探测（纯本地、瞬时）跳过空闲端口，只有被真正占用的端口才 connect 验证身份；`_wait_ready` 同样先判端口再探活。实测冷启动 2.09s / 热启动 1.94s / 窗口出现 2.86s
- [x] **运行形态下发**：新增 `GET /api/v1/meta`（app_mode / version / build_tag / daily_limit / has_server_key），前端据此切换界面，避免维护两套代码
- [x] **双形态界面**（同一套代码按形态差异化）：
  - 桌面版：顶栏紧凑 App 化（隐藏 GitHub/Gitee 等外部跳转）、显示「桌面版」徽标、首页文案强调数据只在本机
  - 网页版：保留开源仓库引流、显示「网页版」徽标与「每日免费 N 次」配额提示、文案强调匿名会话隔离
  - 新增 `frontend/src/composables/useAppMode.js`（单例，仅首次请求 /meta，失败按桌面版兜底）
- [x] **首页重写**（landing + 顶部固定快入口）：滚动时快入口条吸附在顶栏下方，一键直达诊断；并修正过时文案——删除「实时岗位爬取」「语义向量匹配」（M1 已移除），模板数由 3 套更正为 10 套，工作流改为实际 6 节点（含自省精修、标注并行分支）
- [x] 前端 npm run build 过；浏览器实测首页/顶栏无溢出与截断、快入口吸附位置正确（top=64 紧贴 header、z-40 低于 header 的 z-50）、无 JS 报错
- [x] pytest 15 过 + 重新打包 exe + 窗口模式烟测通过

## M9 清理与结构优化（2026-09-21）

- [x] **回收 1.8 GB 磁盘**：`release/` 历次构建的 `.old-*` 产物堆积到 1.96 GB（每次构建留 ~160MB 目录+压缩包），清理后仅保留当前产物，降到 0.16 GB
- [x] **修复产物堆积根因**：`build_desktop.py` 新增 `_prune_old_artifacts()`——旧产物仍先「改名挪开」以规避本机批量删除保护，但构建完只保留最近 1 份作为回退，更早的定向清理
- [x] **删除废弃代码**：`app/agents/nodes/clarifier_agent.py`（追问逻辑实际由 `interactive_opt_agent.generate_questions` 承担，该文件全仓无任何引用）；`scripts/run_diagnosis_test.py`（早期手工联调脚本，已被 pytest 与 `evaluate.py` 取代）
- [x] **删除过期文档**：`docs/项目分析报告.md`（基线 c26d913，整篇描述 MySQL + Playwright 爬取 + sentence-transformers 混合匹配，均为 M1 已删除的能力，属误导性文档）；`docs/ResuMatch-AI-技术说明文档.html`（v1 静态 HTML 副本，与已重写的 md v2.0 重复且含 20 处过时表述）
- [x] **删除模板残留**：`frontend/README.md`（Vite 脚手架默认说明）；`backend/data/test_resume.docx`（无任何代码引用的手工测试素材）
- [x] **git 结构整理**：`.idea/` 早已写入 .gitignore 但仍被跟踪（历史遗留），执行 `git rm -r --cached .idea` 取消跟踪，本地 IDE 配置保留在磁盘
- [x] **文档与代码对齐**：修正 4 处指向已删 `clarifier` 的表述、pytest 计数 14→15、`/api/v1/meta` 与 `/api/v1/live/stream` 补进 API 一览、README 目录树补 `scripts/` 与 `composables/`、技术说明文档「已知问题」表按 M6/M8 完成情况重写
- [x] 回归：pytest 15 过 + npm run build 过 + 脚本语法检查过 + 服务端冒烟（health / meta / templates / 静态页 全 200）
- [x] **测试素材隔离**：`tests/conftest.py` 在导入 app 模块前把 `DB_URL` / `PHOTOS_DIR` 指向 `backend/tests/.tmp/`（带自检断言，隔离失效立即报错）；此前测试直接写开发库 `backend/data/resumatch.db` 与 `data/photos/`，跑一次测试就污染一次运行时数据
- [x] 清理历史测试污染与缓存：`backend/data/photos/` 遗留测试照片、`tests/__pycache__` 中已删测试（test_feature_extractor / test_ranker）的 .pyc、`.pytest_cache/`；`.gitignore` 补 `backend/tests/.tmp/`

## M10 清理盘点 + 预览保真（2026-09-21）

- [x] **缓存清理**：全仓 9 个 `__pycache__` + `backend/.pytest_cache/`（可再生）；`release/` 仅 1 版合规、`backend/tests/.tmp/` 已空、`frontend/dist` 为当前打包产物保留
- [x] **死代码排查（全部保留）**：`watermark.py`（main.py / health.py 引用的作者溯源）、`/_sig` 端点（签名中间件配套）、`city` 参数（pipeline 与提示词实际使用）
- [x] **文档核查**：无误导性残留（MySQL/爬虫字样均为历史记录与前后对比表）；技术说明文档修正「改造计划 M1-M5」→ M1-M10、已知问题表注释指向修正
- [x] **I-01 预览保真解决**：编辑器预览不再用 HTML 语义近似，改为调用 `POST /resumes/export-docx` 生成真实 Word 文件并用 docx-preview 直渲，预览与下载版式完全一致（含真实分页）
  - `buildWordData()` 转换逻辑从 downloadWord 提取，预览与导出共用同一份数据转换
  - 编辑内容 / 模板 / 照片变化 → 600ms 防抖重新生成；seq 序号丢弃过期响应；失败显示错误并保留页面
  - `ResumePreview.vue` 保留用于打印（window.print）链路
  - 前端新增依赖 `docx-preview`
- [x] 验证：npm run build 过；后端冒烟 export-docx 200（37KB docx）；浏览器实测编辑器页 docx 渲染 1 页、无报错、加载态正常

## M11 RAG 证据接地 + 动态多轮追问（2026-09-21，全部完成）

> 目标：从「纯 prompt 的 API 调用」升级为「证据接地 + 人在回路」的诊断框架，支撑毕设技术复杂度。

### A. RAG 证据接地
- [x] **A1 检索层** `app/core/evidence.py`：简历/JD 条目级分块（bullet 行独立成块，R1/J1 稳定 id）→ 向量化 → task 内纯 Python 余弦 Top-K；embedding 走 OpenAI 协议 `/embeddings`（默认 text-embedding-3-small，可配 `LLM_EMBEDDING_MODEL`），不可用自动降级 CJK bigram 关键词重叠（进程级熔断防重复超时）
- [x] **A2 链路接入**：scorer 按六维度检索注入证据池并输出 `evidence_ids`；gap 以 JD 原文块为查询交叉检索（证据池 ≤10 条），差距条目挂 `evidence` 原文片段，无引用自动标 `is_inferred`；rewriter 要求忠于证据原文、不得编造，建议输出 `evidence_ids`
- [x] **A3 前端**：差距条目展示引用原文片段（[R3] 前缀）+「推断」角标；改写建议显示依据证据 id

### B. 动态多轮追问（人在回路，解决 I-03 固定轮次）
- [x] **B1 clarify_agent**：plan/wait 两节点。plan 基于差距+解析+岗位要求生成 0-3 个问题（信息充分返回空，不为问而问；失败跳过不熔断），先提交 state 再 interrupt（恢复时无重复 LLM 调用）；wait 原生同步节点，有 thread_id 才 interrupt
- [x] **B2 图编排**：gap → clarify_plan →（有问题→clarify_wait interrupt / 无问题→rewriter）→ rewriter → refine。交互图 `build_interactive_diagnosis_graph()`（MemorySaver + 同步节点包装）由 `asyncio.to_thread` 驱动同步 stream —— **Python 3.10 的 async 图中 interrupt 依赖的 contextvar 不可用（langgraph 要求 3.11+），同步路径是官方兼容解**；离线图（无 checkpointer）保持 async 节点，行为不变
- [x] **B3 API/前端**：`POST /live/clarify/{task_id}`（校验 owner + waiting_clarify 状态）→ `Command(resume)` 恢复；SSE/轮询 payload 增加 `questions`；ResultView 追问卡片（textarea 收集回答）→ 提交后续跑改写/精修并正常落库

### C. 评估（论文实验章）
- [x] evaluate.py 扩展（`--ablation` + 稳定性两模式）：证据引用指标（差距引用率/推断率/建议带据率）+ 消融对比（完整 / 无RAG / 无refine / 在线自动追问）+ 输出消融报告（M13 起并入技术说明文档第 13 章）
- [x] 真实 LLM 验证（zhipu glm-4-flash，embedding 余额不足自动降级关键词检索）：E2E 两轮通过（证据引用 + 追问暂停/恢复全链路）；稳定性 3 用例 × 2 次 overall 80.8±1.0
- [x] 消融结果（case-1）：A 完整 82 分/引用率 100%；B 无RAG 74 分/引用率 0%/推断率 100%/差距数 3→6 —— RAG 证据接地贡献 +8 分且消除无据推断；refine 贡献 +1 分
- [x] 评估方法学修正：B 变体仅禁检索不够 —— 空池下模型会幻觉出恰好合法的证据 id（filter_valid_ids 对完整索引校验放行，实测引用率虚高 100%），需同时禁用证据校验才构成有效消融
- 已知边界：暂停任务仅存于进程内存（服务重启后恢复失败会一直 running，10 分钟 SSE 兜底断开）；快照随任务终态 delete_thread 释放（→ M16 已解决，见下文）

## M12 检索质量与接地稳健性优化（2026-09-21）

- [x] **关键词检索 IDF 加权**（`evidence.py`）：旧实现纯重叠计数，'负责/项目' 等高频词与区分词同权；改为 `IDF 加权和 × √查询覆盖度 ÷ √块长度`（BM25-lite，DF 表缓存在 index 上），区分性证据块排名提升
- [x] **空池防幻觉指令**（gap/rewriter prompt）：证据池为空或无相关证据时 evidence_ids 必须留空且 is_inferred=true，严禁编造 id —— 针对消融实验发现的「空池下模型幻觉出恰好合法 id」问题的 prompt 层缓解（filter_valid_ids 校验仍兜底）
- [x] 验证：pytest 22 过；E2E 复跑差距引用 6/6（优化前 5/6 + 1 推断）；消融复跑 A vs B 结论稳定（82 vs 74 分、引用率 100% vs 0%、推断率 ~0% vs 100%），单次运行存在 ±3 分 LLM 随机抖动属正常

## M13 健壮性与文档收敛（2026-09-21）

- [x] **重复诊断防护**：同 owner 已有 pending/running/waiting_clarify 任务时 `POST /live/analyze` 返回 409（`LivePipelineService.has_active_task`），防双击重复发起浪费 LLM 调用
- [x] **追问回答入参加固**：单条截断 500 字（防超长回答撑爆 prompt），保留去空/≤10 条校验
- [x] **代码简化**：`models/schemas.py` 仅剩 HealthOut 一个模型 → 并入 `api/v1/health.py`，删除空壳文件
- [x] **文档收敛为三件套**：技术说明文档补齐 M11-M13（RAG/追问/interrupt 章节 + 第 12 章踩坑精华 + 第 13 章质量评估）；问题解决记录/评估报告/消融评估报告 三文档并入后删除；`毕设-技术知识点梳理.md` 转正为 `docs/论文准备.md` 入库
- [x] 验证：pytest 全过

## M14 导出体验升级（2026-09-21）

- [x] 桌面端 Word 导出原生「另存为」对话框选路径（`desktop.py` DesktopApi js_api 桥 + `export-docx` 的 `save_path` 直写，仅桌面形态放行）；网页态保持浏览器下载
- [x] 导出历史：`export_history` 表 + `GET/DELETE /resumes/export-history`，编辑器顶栏弹层（最近 30 条，桌面态可「打开位置」）
- [x] PDF 修复：打印直印 docx 预览 DOM（与屏幕预览/Word 导出版式一致），未就绪回落旧模板
- [x] 验证：pytest 22 过、接口冒烟过；已重新打包 exe（fbb477f）

## M15 独立简历创建 + 仓库入口（2026-09-21）

- [x] 编辑器独立创建模式：新增路由 `/editor`（无 taskId），空白起步直接编写，自动存 `resume_edit_blank`；原 `/editor/:taskId` 诊断优化链路保留，两模式存档键隔离
- [x] 首页新增「✍️ 免费创建简历」入口；编辑器顶栏标题区分模式、无任务隐藏「← 返回报告」
- [x] 首页 CTA 区新增 GitHub / Gitee 开源仓库跳转（官方 SVG 图标）
- [x] 测试素材：`backend/tests/fixtures/` 新增 2 份 docx 简历（高/低匹配对照）+ 共用 JD 文本
- [x] 验证：`npm run build` 过；docx 解析冒烟过

## M16 跨重启恢复 + 草稿暂存（2026-09-21）

- [x] **SqliteSaver 持久化**（`graph.py`）：交互图 checkpointer 从 MemorySaver 升级为 SqliteSaver（`data/checkpoints.db`，`check_same_thread=False` 适配 worker 线程同步 stream），interrupt 暂停的图状态落文件快照；快照库初始化失败自动降级 MemorySaver（功能退化不阻塞诊断）
- [x] **waiting 任务落库**（`live_pipeline_service.py` + `entities.py`）：`diagnosis_records` 新增 `questions` / `clarify_ctx` JSON 列（老库经 `_SCHEMA_NEW_COLUMNS` 自动补列），进入 waiting_clarify 时把追问问题与恢复上下文（jd_text/city/llm_config/enable_refine 等）一并落库
- [x] **跨重启恢复链路**：`resume()` 发现内存任务表无该条目时自动从 DB 重建（`_restore_waiting_task`）；`main.py` lifespan 启动补偿扫描 waiting_clarify 记录重建进内存表（`recover_waiting_tasks`）；无 clarify_ctx 的老版本遗留任务优雅标记 failed（提示重新发起）而非悬置——M11「暂停任务仅存于进程内存」边界就此关闭
- [x] **前端追问草稿暂存**（`ResultView.vue`）：回答内容实时存 `localStorage clarify_draft_{taskId}`，误关页面/重启后回来不丢已填答案，提交成功后清除
- [x] 打包清单补 `langgraph.checkpoint.sqlite` / `sqlite_vec` hiddenimports
- [x] 新增 `test_live_recovery.py` 3 项跨重启集成测试（快照落库+模拟重启恢复续跑、启动补偿、legacy 降级）；conftest 启动时清理快照库残留；scorer 并行化经核实为伪优化（单次 LLM 调用评六维，检索纯 CPU 毫秒级）不做
- [x] 验证：pytest 25 过（22+3）；`npm run build` 过

## M17 Web 形态压力摸底（2026-09-22）

- [x] 新增 `backend/scripts/stress_web.py`：进程内起 uvicorn（随机端口）+ mock LLM 拉长任务窗口，① 单任务阶梯开 25/50/100/150 条 SSE 连接测首包延迟与内存 ② 20/60/120 工作循环并发插入测 WAL 写吞吐与锁错误；数据全部落 `tests/.tmp/stress/`
- [x] 结果：SSE 150 并发全连上、首包 p95 < 0.5s、0 错误、内存 218MB（+0.6MB/连接）；WAL 写吞吐 ~350-400 ops/s（瓶颈在连接池排队）、busy_timeout 下偶发 locked 约 0.03% —— 详见技术说明文档 13.4
- [x] 客户端与服务端同进程使结果偏保守，作下界参考（诚实注明测法局限）
- [x] 文档同步：技术说明文档 13.4 新增压测章节 + 13.5 展望勾掉已完成项；论文准备第十一章同步

## M18 产品化改造一期：统一壳 + 双主题 + 上手闭环（2026-09-22）

> 依据 [`产品化完善计划.md`](产品化完善计划.md) 走查第 13/14/16/17 环节，实施 D0/D1/D5/A1/D2/F2/A5/A6。

- [x] **D1 主题 token 地基**：tailwind 语义 token（page/panel/float/inset/line/ink 三阶/accent/ok/warn/bad，RGB 三元组支持透明度修饰符）+ style.css 亮暗双色板 CSS 变量 + Arco 品牌紫暗色阶；`useTheme` 组合式（auto 跟随系统 / 手动选择存 localStorage / `data-theme` 切换 / body arco-theme 联动）；index.html 内联脚本应用挂载前定主题防闪屏
- [x] **D0 首页/功能页分离 + 统一应用壳**：App.vue 重构为 ConsoleShell——左侧边栏（介绍/发起诊断/创建简历/简历库(占位)/诊断历史/设置/更新日志，底部主题切换+GitHub/Gitee+版本号+配额徽标）+ 窄屏顶栏兜底；路由重组 `/`=纯介绍页、`/app/*`=工作台（旧路径全部 redirect 兼容）；桌面端启动经 meta 判定直进 `/app/analyze`（仅一次，不劫持回介绍页）
- [x] **A4 创建简历入口**：侧边栏一级导航项（/app/editor）
- [x] **D5 空态/加载态统一组件**：`EmptyState`（title/desc/type+插槽）/`LoadingBlock`（text/hint），全站手写空态替换
- [x] **A1 粘贴文本简历 + 示例一键体验**：后端 `POST /resumes/upload-text`（ResumeService.save_text，50-5 万字校验）；分析页「上传文件/粘贴文本」双 Tab + 「▶ 填入示例简历」一键填入（`src/data/example.js` 内置低配简历+JD）；首页「30 秒看示例」带 `?demo=1` 直达自动填充
- [x] **D2 分析页重排**：`max-w-2xl` 窄栏 → `lg:grid-cols-2` 双栏（左=简历输入、右=JD+提交 sticky），1440px 一屏完成输入
- [x] **F2 JD 复用**：最近 5 条存 localStorage（首行摘要标题），分析页 JD 区一键选用/删除，提交成功自动记录
- [x] **A5 新用户 Key 引导**：`useKeyGuide` 全局状态（本地无 Key 且服务端未预置 → 引导层三步指引，可「稍后再说」本会话不再弹）；侧边栏设置项常驻小红点直到配置成功；分析页提交前拦截未配置直接弹引导不发请求；保存/清空配置后自动刷新引导状态
- [x] **A6 主流模型预设 + 连通性测试**：厂商预设 10 家（智谱/DeepSeek/通义千问/Kimi/豆包/OpenAI/硅基流动/OpenRouter/Ollama 本地/自定义），自动填 base_url + 推荐模型列表（datalist+快捷 chip）+ 控制台直达链接；标注向量检索支持（不支持时提示 RAG 自动回退关键词检索）；`test-llm` 端点错误按类型归因（Key 无效/余额不足/限流/Base URL 或模型名不存在/网络不通）并附原始错误
- [x] **全站视图 token 化**：Result/Chat/History/Editor/Changelog/ResumePreview 拟物阴影与裸色值全部替换为语义 token，路由字符串同步 /app 前缀，空态组件化
- [x] 验证：pytest 全量过（含新增 test_upload_resume_text）；`npm run build` 过

## M19 岗位市场：一键获取岗位 + 按简历智能推荐（2026-09-23）

> 依据 [`产品化完善计划.md`](产品化完善计划.md) F5（用户新增项），实施于 M18 之后。

- [x] **数据源适配层**：`services/job_market.py`——JobProvider 模式（Mock / JSearch(RapidAPI) / Adzuna），统一 `JobItem` 归一化、`JobProviderError` 错误分类（invalid_key/quota/network/unknown）；只接官方/三方数据 API 不爬虫；国内候选（聚合招聘接口维护中、天行/极速无岗位检索）核实后替换为真实可用源
- [x] **推荐链路**：`POST /jobs/search`（关键词+城市检索）+ `POST /jobs/recommend`（Jaccard 关键词重合度粗筛 → LLM 精排打分+理由，结构化输出+池内 id 过滤防幻觉 → LLM 不可用降级纯关键词排序）
- [x] **数据源测试端点**：`POST /settings/test-job` 按错误类型归因，与 test-llm 同款
- [x] **设置页「招聘数据源」区块**：预设 + 控制台直达外链 + Key/App ID + 测试连接 + 保存（localStorage `job_source_config`，Key 仅存本机）
- [x] **分析页「⚡ 一键获取岗位」面板**：粘贴简历 ≥100 字自动切推荐模式（匹配分+理由），岗位卡片「用此岗位诊断」一键填入 JD（衔接 F2 复用栈）+ 原文外链
- [x] 验证：pytest 全量过（40 用例，含新增 14）；`npm run build` 过

## 环境与约定（备忘）

- 后端 conda 环境（真实路径）：`E:\conda\envs\resumatch-ai\python.exe`（`C:\Users\许\.conda\envs\resumatch-ai` 是 junction，可能被清理工具删除）；测试：`cd backend && python -m pytest tests/`
- 环境若被清理工具破坏：`Lib/` 标准库与 `DLLs/` 可用 python.org 嵌入包（3.10.11）补齐，`site-packages` 通常完好；`pip` 用 get-pip.py 的 `--target` 方式绕过 `Scripts\*.exe` 被杀软拦截的问题
- 测试隔离：conftest 在导入 app 前把 `DB_URL` / `PHOTOS_DIR` 指向 `backend/tests/.tmp/`（自检断言，隔离失效即报错），并手动 create_all + ensure_schema_columns（ASGITransport 不触发 lifespan）；测试不再写入开发库 `backend/data/resumatch.db`
- 老库升级模式：新增列统一登记在 `app/core/db.py` 的 `_SCHEMA_NEW_COLUMNS`
- 本机 8765 端口常被「蓝笔申论 BluePencil」占用，ResuMatch 会顺延 8766+
- 前端路由为 hash 模式（`#/analyze`），自动化导航必须带 `#`
- SQLite 时间戳为 UTC naive；配额/启动补偿的日期比较均按 UTC
